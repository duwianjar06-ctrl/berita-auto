name: Temporary repair runner (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary repair runner disabled; production scheduler unchanged.'
