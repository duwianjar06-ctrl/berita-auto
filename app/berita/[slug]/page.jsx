import {notFound,permanentRedirect} from 'next/navigation';
import Header from '../../../components/Header.jsx';
import Footer from '../../../components/Footer.jsx';
import ArticleViewTracker from '../../../components/ArticleViewTracker.jsx';
import ArticleShare from '../../../components/ArticleShare.jsx';
import {readPublicArticles} from '../../../lib/public-data.js';
import {articlePath,articleSlug,stableIdFromArticlePath,matchesArticleStableId} from '../../../lib/article-url.js';
import {isLegacyArticlePath,legacyIdFromPath,resolveLegacyArticle} from '../../../lib/legacy-route.js';
import {cleanExcerpt} from '../../../lib/text.js';
import {getPopularArticles} from '../../../lib/analytics.js';
import {FOREIGN_SOURCE_IDS} from '../../../lib/sources.js';
import {categories} from '../../../lib/categories.js';
import {hasRealSourceImage} from '../../../lib/homepage-display.js';
import AdSlot from '../../../components/AdSlot.jsx';
const base='https://berita-auto.vercel.app';
export const dynamic='force-dynamic';
function safeArray(value){return Array.isArray(value)?value:[]}
function formatDate(value){if(!value)return '-';const d=new Date(value);return Number.isNaN(d.getTime())?'-':new Intl.DateTimeFormat('id-ID',{dateStyle:'medium',timeStyle:'short',timeZone:'Asia/Jakarta'}).format(d)+' WIB';}
function paragraphs(content){return String(content||'').trim().split(/\n{2,}/).map(p=>p.trim()).filter(Boolean);}
function semanticCategory(value='Nasional'){return String(value).toLowerCase().replace(/\s+/g,'-');}
function isForeign(article){return String(article?.language||'id').toLowerCase()!=='id'||FOREIGN_SOURCE_IDS.has(String(article?.sourceId||''));}
function isTranslationPending(article){return String(article?.translationStatus||'').toLowerCase()==='pending'||(isForeign(article)&&String(article?.translationStatus||'').toLowerCase()!=='translated');}
function categoryCounts(items){const counts=Object.fromEntries(categories.map(category=>[category,0]));for(const article of safeArray(items)){const category=categories.find(name=>name.toLowerCase()===String(article?.category||'').trim().toLowerCase());if(category)counts[category]+=1}return counts}
export async function generateMetadata({params}){const {slug}=await params;const items=safeArray(await readPublicArticles());const direct=items.find(x=>x?.id===slug);const article=direct||items.find(x=>matchesArticleStableId(x,slug));if(!article&&isLegacyArticlePath(`/berita/${slug}`))return{title:'Artikel tidak ditemukan',description:'Artikel tidak tersedia.',robots:{index:false,follow:true,noarchive:true},openGraph:{type:'website',title:'Artikel tidak ditemukan',description:'Artikel tidak tersedia.',siteName:'Berita Auto',locale:'id_ID'},twitter:{card:'summary',title:'Artikel tidak ditemukan',description:'Artikel tidak tersedia.'}};if(!article)return{title:'Artikel tidak ditemukan',robots:{index:false,follow:true,noarchive:true}};const canonical=`${base}${articlePath(article)}`;const description=cleanExcerpt(article.excerpt||article.summary||article.title||'').slice(0,160);const pending=isTranslationPending(article);const image=hasRealSourceImage(article)?String(article.imageUrl):null;const imageMeta=image?[{url:image,width:1200,height:800,alt:article.title}]:[];return{title:article.title,description,robots:pending?{index:false,follow:true,noarchive:true}:{index:true,follow:true},alternates:{canonical},openGraph:{type:'article',url:canonical,title:article.title,description,siteName:'Berita Auto',locale:'id_ID',publishedTime:article.sitePublishedAt||article.publishedAt||article.createdAt,modifiedTime:article.updatedAt||article.sitePublishedAt||article.publishedAt,images:imageMeta},twitter:{card:'summary_large_image',title:article.title,description,images:image?[image]:[]}}}
export default async function NewsDetailPage({params}){const {slug}=await params;const items=safeArray(await readPublicArticles());const direct=items.find(x=>x?.id===slug);if(direct)permanentRedirect(articlePath(direct));if(isLegacyArticlePath(`/berita/${slug}`)){const resolved=await resolveLegacyArticle(items,legacyIdFromPath(`/berita/${slug}`));if(resolved?.article)permanentRedirect(articlePath(resolved.article));notFound()}const short=stableIdFromArticlePath(slug);if(!short)notFound();const article=items.find(x=>matchesArticleStableId(x,slug));if(!article)notFound();if(slug!==articleSlug(article))permanentRedirect(articlePath(article));const body=paragraphs(article.content);if(!body.length)notFound();const category=String(article.category||'Nasional');const categoryUrl=`${base}/kategori/${semanticCategory(category)}`;const published=items.filter(x=>!isForeign(x)&&!isTranslationPending(x));const sameCategory=published.filter(x=>x?.id!==article.id&&String(x?.category||'')===String(article.category||''));const related=(sameCategory.length?sameCategory:published.filter(x=>x?.id!==article.id)).slice(0,5);const popular=safeArray(await getPopularArticles(items,7,5));const readingMinutes=Math.max(1,Math.ceil(body.join(' ').split(/\s+/).filter(Boolean).length/220));const sourceTime=article.sourcePublishedAt||article.publishedAt;const siteTime=article.sitePublishedAt||article.createdAt;const standfirst=cleanExcerpt(article.excerpt||article.summary||'');const articleImage=hasRealSourceImage(article)?String(article.imageUrl):'';const authorName=String(article.author||article.authorName||'').trim();const canonical=`${base}${articlePath(article)}`;const schema={'@context':'https://schema.org','@type':'NewsArticle',headline:String(article.title||''),description:standfirst,datePublished:siteTime||sourceTime,dateModified:article.updatedAt||siteTime||sourceTime,mainEntityOfPage:{'@type':'WebPage','@id':canonical},url:canonical,publisher:{'@type':'Organization',name:'Berita Auto',url:base},image:articleImage?[articleImage]:undefined,...(authorName?{author:{'@type':'Person',name:authorName}}:{})};const breadcrumbSchema={'@context':'https://schema.org','@type':'BreadcrumbList',itemListElement:[{'@type':'ListItem',position:1,name:'Beranda',item:base},{'@type':'ListItem',position:2,name:category,item:categoryUrl},{'@type':'ListItem',position:3,name:article.title,item:canonical}]};const counts=categoryCounts(items);return <><ArticleViewTracker articleId={article.id}/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(breadcrumbSchema)}}/><Header latestTitle={published[0]?.title||article.title} counts={counts}/><main className="article-layout"><article className="article-main-column"><nav className="article-breadcrumbs" aria-label="Breadcrumb"><a href="/">Beranda</a><span aria-hidden="true">›</span><a href={`/kategori/${semanticCategory(category)}`}>{category}</a><span aria-hidden="true">›</span><span aria-current="page">Berita</span></nav><div className="article-heading-meta"><p className="article-category"><a href={`/kategori/${semanticCategory(category)}`}>{category}</a><span aria-hidden="true">•</span><strong>{article.publisher||article.sourceName||'Sumber publik'}</strong></p><h1 className="article-title fade-up">{article.title}</h1></div><p className="article-standfirst fade-up">{standfirst}</p><div className="article-meta fade-up"><span><strong>Sumber terbit:</strong> {formatDate(sourceTime)}</span><span>•</span><span><strong>Terbit di Berita Auto:</strong> {formatDate(siteTime)}</span>{article.updatedAt&&article.updatedAt!==siteTime&&<><span>•</span><span><strong>Diperbarui:</strong> {formatDate(article.updatedAt)}</span></>}<span>•</span><span>{readingMinutes} menit baca</span></div>{articleImage&&<figure className="article-hero soft-scale"><img src={articleImage} alt={article.title} width="1200" height="800" loading="eager" fetchPriority="high"/>{article.imageCaption&&<figcaption>{article.imageCaption}</figcaption>}</figure>}<div className="article-body">{body.map((p,i)=><div key={`${article.id}-${i}`}>{i===3&&<AdSlot variant="inArticle" placement="article"/>}<p>{p}</p></div>)}</div><ArticleShare title={article.title} url={canonical}/><div className="article-source"><strong>Sumber:</strong> {article.sourceName||article.publisher||'Sumber publik'} {article.url&&<a href={article.url} target="_blank" rel="noopener noreferrer">Baca artikel sumber ↗</a>}</div><section className="article-related"><p className="category-eyebrow">LANJUTKAN MEMBACA</p><h2>Berita Terkait</h2><div className="article-related-grid">{related.map(a=><a className="article-related-card" key={a.id} href={articlePath(a)}>{hasRealSourceImage(a)&&<img src={a.imageUrl} alt={a.title} width="600" height="400" loading="lazy"/>}<div className="article-related-body"><span className="category-chip">{a.category||'Nasional'}</span><h3>{a.title}</h3></div></a>)}</div></section></article><aside className="article-sidebar"><section className="article-side-panel"><span className="category-eyebrow">TERBARU</span><h3>Berita Terbaru</h3><div className="article-side-list">{published.filter(a=>a.id!==article.id).slice(0,5).map(a=><a key={a.id} href={articlePath(a)}>{a.title}</a>)}</div></section>{popular.length>0&&<section className="article-side-panel"><span className="category-eyebrow">POPULER 7 HARI</span><h3>Paling Banyak Dilihat</h3><div className="article-side-list">{popular.filter(a=>!isForeign(a)&&a.id!==article.id).map((a,i)=><a key={a.id} href={articlePath(a)}><b>{i+1}.</b> {a.title}<small>{a.views} views</small></a>)}</div></section>}<AdSlot variant="rectangle" placement="sidebar"/><section className="article-side-panel"><span className="category-eyebrow">KATEGORI</span><h3>{category}</h3><a href={`/kategori/${semanticCategory(category)}`}>Lihat semua berita kategori →</a></section></aside></main><Footer/></>}
