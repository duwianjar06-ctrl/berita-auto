import {notFound,permanentRedirect} from 'next/navigation';
import {readArticles} from '../../../lib/storage.js';
import {articlePath,articleSlug} from '../../../lib/article-url.js';
import AdSlot from '../../../components/AdSlot.jsx';
const base='https://berita-auto.vercel.app';
export const dynamic='force-dynamic';
function formatDate(value){if(!value)return '-';const d=new Date(value);return Number.isNaN(d.getTime())?'-':new Intl.DateTimeFormat('id-ID',{dateStyle:'medium',timeStyle:'short',timeZone:'Asia/Jakarta'}).format(d)+' WIB';}
function paragraphs(content){return String(content||'').trim().split(/\n{2,}/).map(p=>p.trim()).filter(Boolean);}
export async function generateMetadata({params}){const {slug}=await params;const items=await readArticles();const old=items.find(x=>x.id===slug);const short=/^(.+)-([a-f0-9]{8})$/.exec(slug)?.[2]||'';const article=old||items.find(x=>String(x.fingerprint||x.id).startsWith(short));if(!article)return {title:'Artikel tidak ditemukan'};const canonical=`${base}${articlePath(article)}`;const description=(article.excerpt||article.summary||article.title||'').slice(0,160);return {title:article.title,description,alternates:{canonical},openGraph:{type:'article',url:canonical,title:article.title,description,siteName:'Berita Auto',locale:'id_ID',publishedTime:article.publishedAt||article.createdAt,images:article.imageUrl?[{url:article.imageUrl}]:[]},twitter:{card:'summary_large_image',title:article.title,description,images:article.imageUrl?[article.imageUrl]:[]}};}
export default async function ArticlePage({params}){const {slug}=await params;const items=await readArticles();const old=items.find(x=>x.id===slug);if(old)permanentRedirect(articlePath(old));const match=/^(.+)-([a-f0-9]{8})$/.exec(slug);if(!match)notFound();const article=items.find(x=>String(x.fingerprint||x.id).startsWith(match[2]));if(!article)notFound();if(slug!==articleSlug(article))permanentRedirect(articlePath(article));const canonical=`${base}${articlePath(article)}`;const related=items.filter(x=>x.id!==article.id&&(x.category||'')===(article.category||'')).slice(0,6);const body=paragraphs(article.content||article.excerpt||article.summary||'');const readingMinutes=Math.max(1,Math.ceil(body.join(' ').split(/\s+/).length/220));const shareWhatsApp=`https://wa.me/?text=${encodeURIComponent(`${article.title} ${canonical}`)}`;const schema={'@context':'https://schema.org','@type':'NewsArticle',headline:article.title,description:article.excerpt||article.summary||'',datePublished:article.publishedAt||article.createdAt,dateModified:article.updatedAt||article.publishedAt||article.createdAt,mainEntityOfPage:{'@type':'WebPage','@id':canonical},url:canonical,publisher:{'@type':'Organization',name:'Berita Auto',url:base},image:article.imageUrl?[article.imageUrl]:undefined};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><main className="article-layout"><article className="article-main-column"><p className="article-category"><a href={`/kategori/${(article.category||'nasional').toLowerCase()}`}>{article.category||'Nasional'}</a></p><h1 className="article-title">{article.title}</h1><p className="article-standfirst">{article.excerpt||article.summary||''}</p><div className="article-meta"><span><strong>Sumber:</strong> {article.sourceName||'Sumber publik'}</span><span>•</span><span><strong>Dipublikasikan:</strong> {formatDate(article.publishedAt||article.createdAt)}</span>{article.updatedAt&&article.updatedAt!==article.publishedAt&&<><span>•</span><span><strong>Diperbarui:</strong> {formatDate(article.updatedAt)}</span></>}<span>•</span><span>{readingMinutes} menit baca</span></div>{article.imageUrl&&<figure className="article-hero"><img src={article.imageUrl} alt={article.title} width="1200" height="800" loading="eager"/></figure>}<div className="article-body">{body.map((p,i)=><div key={`${article.id}-${i}`}>{i===3&&<AdSlot variant="inArticle" placement="article"/>}<p>{p}</p></div>)}</div><div className="article-share"><a href={shareWhatsApp} target="_blank" rel="noopener noreferrer">Bagikan WhatsApp</a><a href={canonical} onClick={undefined}>Salin Link</a></div><div className="article-source"><strong>Sumber:</strong> {article.sourceName||'Sumber publik'} {article.url&&<a href={article.url} target="_blank" rel="noopener noreferrer">Baca artikel sumber ↗</a>}</div><section className="article-related"><p className="category-eyebrow">LANJUTKAN MEMBACA</p><h2>Berita Terkait</h2><div className="article-related-grid">{related.map(a=><a className="article-related-card" key={a.id} href={articlePath(a)}>{a.imageUrl&&<img src={a.imageUrl} alt={a.title} width="600" height="400" loading="lazy"/>}<div className="article-related-body"><span className="category-chip">{a.category||'Nasional'}</span><h3>{a.title}</h3></div></a>)}</div></section></article><aside className="article-sidebar"><section className="article-side-panel"><span className="category-eyebrow">TERBARU</span><h3>Berita Terbaru</h3><div className="article-side-list">{items.slice(0,5).map(a=><a key={a.id} href={articlePath(a)}>{a.title}</a>)}</div></section><AdSlot variant="rectangle" placement="sidebar"/><section className="article-side-panel"><span className="category-eyebrow">KATEGORI</span><h3>{article.category||'Nasional'}</h3><a href={`/kategori/${(article.category||'Nasional').toLowerCase()}`}>Lihat semua berita kategori →</a></section></aside></main></>}
