import {readArticles} from '../lib/storage.js';
import {categories} from '../lib/categories.js';
export default async function Home(){const articles=await readArticles();return <main className="container"><header><div><h1>Berita Auto</h1><p>Portal berita otomatis berbasis AI</p></div><strong>● AUTO 5 MENIT</strong></header><nav>{categories.map(c=><a key={c} href={'#'+c}>{c}</a>)}</nav><section className="hero"><small>UPDATE OTOMATIS</small><h2>Berita terbaru dikumpulkan setiap 5 menit.</h2><p>Sumber RSS diproses, dideduplikasi, dikategorikan, lalu ditulis ulang menjadi artikel.</p></section><section className="grid">{articles.length?articles.map(a=><article key={a.id}><small>{a.category}</small><h3>{a.title}</h3><p>{a.content}</p><a href={a.url} target="_blank" rel="noreferrer">Sumber ↗</a></article>):<article><h3>Belum ada artikel</h3><p>Worker akan mengisi artikel otomatis.</p></article>}</section></main>}
