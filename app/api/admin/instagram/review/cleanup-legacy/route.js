import {NextResponse} from 'next/server';import {auth} from '../../../../../../auth.js';import {cleanupLegacyInstagramBatch} from '../../../../../../lib/instagram-review-cleanup.js';import {invalidateInstagramAdminSnapshot} from '../../../../../../lib/instagram-admin-snapshot.js';
export const dynamic='force-dynamic';export const runtime='nodejs';
async function authorized(){let session=null;try{session=await auth()}catch{return false}const allowed=(process.env.ADMIN_EMAILS||'').split(',').map(v=>v.trim().toLowerCase()).filter(Boolean);const email=session?.user?.email?.trim().toLowerCase();return Boolean(email&&allowed.includes(email));}
export async function POST(request){if(!await authorized())return NextResponse.json({error:'Unauthorized'},{status:401,headers:{'Cache-Control':'no-store'}});try{const body=await request.json().catch(()=>({}));const cutoverAt=String(body?.cutoverAt||'');const dryRun=body?.dryRun!==false;const result=await cleanupLegacyInstagramBatch({cutoverAt,dryRun,removedBy:'admin-cutover'});if(!dryRun)invalidateInstagramAdminSnapshot();return NextResponse.json(result,{status:200,headers:{'Cache-Control':'no-store'}})}catch(error){return NextResponse.json({error:String(error?.code||error?.message||error).slice(0,240)},{status:409,headers:{'Cache-Control':'no-store'}})}}
