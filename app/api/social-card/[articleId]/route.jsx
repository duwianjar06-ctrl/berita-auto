import fs from 'node:fs';import path from 'node:path';import {NextResponse} from 'next/server';import {readArticles} from '../../../../lib/storage.js';import {renderSocialCard,SOCIAL_CARD_DESIGN_VERSION} from '../../../../lib/social-card-renderer.js';import {sanitizeCardText} from '../../../../lib/social-visual.js';
export const dynamic='force-dynamic';export const runtime='nodejs';
const fontDir=path.join(process.cwd(),'assets','fonts');const regularFont=path.join(fontDir,'NotoSans-Regular.ttf');const boldFont=path.join(fontDir,'NotoSans-Bold.ttf');const fontconfigDir=path.join('/tmp','berita-auto-fontconfig');const runtimeFontconfig=path.join(fontconfigDir,'fonts.conf');fs.mkdirSync(fontconfigDir,{recursive:true});const esc=v=>String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\"/g,'&quot;').replace(/'/g,'&apos;');fs.writeFileSync(runtimeFontconfig,`<?xml version="1.0"?><!DOCTYPE fontconfig SYSTEM "fonts.dtd"><fontconfig><dir>${esc(fontDir)}</dir><dir>/usr/share/fonts</dir><dir>/usr/local/share/fonts</dir><dir>/opt/fonts</dir><cachedir>/tmp/fontconfig</cachedir></fontconfig>`,'utf8');process.env.FONTCONFIG_FILE=runtimeFontconfig;process.env.FONTCONFIG_PATH=fontconfigDir;
const normalize=v=>Array.isArray(v)?v:Array.isArray(v?.items)?v.items:Array.isArray(v?.values)?v.values:[];
export function normalizeSocialArticle(article){return{...article,title:sanitizeCardText(article?.title||''),topics:normalize(article?.topics),keywords:normalize(article?.keywords),entities:normalize(article?.entities),paragraphs:normalize(article?.paragraphs),slides:normalize(article?.slides)}}
export async function GET(request,{params}){try{const id=decodeURIComponent(String((await params)?.articleId||''));const slide=Math.max(1,Math.min(2,Number(new URL(request.url).searchParams.get('slide')||1)));const raw=(await readArticles()).find(item=>String(item?.id)===id);if(!raw)return new NextResponse('Article not found',{status:404});const rendered=await renderSocialCard(normalizeSocialArticle(raw),slide);return new NextResponse(rendered.buffer,{status:200,headers:{'Content-Type':'image/jpeg','Cache-Control':'public,max-age=300,stale-while-revalidate=600','X-Social-Card-Render':'valid','X-Social-Card-Font-Source':fs.existsSync(regularFont)&&fs.existsSync(boldFont)?'bundled-ttf':'runtime-fallback','X-Social-Card-Design-Version':SOCIAL_CARD_DESIGN_VERSION,'X-Social-Card-Image-Provenance':rendered.provenance,'X-Social-Card-Dimensions':`${rendered.width}x${rendered.height}`}})}catch(error){console.error('[social-card]',error);return NextResponse.json({error:'CARD_RENDER_FAILED',message:String(error?.message||error).slice(0,240)},{status:500,headers:{'Cache-Control':'no-store'}})}}
