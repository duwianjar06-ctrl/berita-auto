import {NextResponse} from 'next/server';import {prepareInstagramProductionQueue} from '../../../../lib/instagram-preparation-runtime.js';import {getInstagramAutomationSettings} from '../../../../lib/instagram-automation.js';import {kickInstagramPublisher} from '../../../../lib/instagram-auto-upload.js';import {withPersistenceSource} from '../../../../lib/persistence.js';import {classifyUpstashQuotaError,qstashDeliveryInfo,qstashNonRetryableResponse} from '../../../../lib/upstash-quota.js';export const dynamic='force-dynamic';export const runtime='nodejs';function authorized(r){const s=process.env.CRON_SECRET||'';return Boolean(s&&(r.headers.get('authorization')||'')===`Bearer ${s}`)}export async function GET(request){if(!authorized(request))return NextResponse.json({error:'Unauthorized'},{status:401});const delivery=qstashDeliveryInfo(request);console.log('[qstash-delivery]',JSON.stringify({route:'/api/cron/social-prepare',...delivery}));try{const merged=await withPersistenceSource('social-prepare',async()=>{const result=await prepareInstagramProductionQueue({trigger:'social-prepare',now:Date.now()});const settings=await getInstagramAutomationSettings();let autoPublish=null;if(settings.autoUploadEnabled&&Number(result?.diagnostics?.readyCreated||0)>0)autoPublish=await kickInstagramPublisher();return{...result,autoUploadEnabled:settings.autoUploadEnabled,autoPublishTriggered:Boolean(settings.autoUploadEnabled&&Number(result?.diagnostics?.readyCreated||0)>0),autoPublish}});console.log('[instagram-prepare]',JSON.stringify(merged));return NextResponse.json(merged,{status:200,headers:{'Cache-Control':'no-store'}})}catch(error){console.error('[instagram-prepare] failed',error);const quotaCode=classifyUpstashQuotaError(error);if(quotaCode){return qstashNonRetryableResponse({ok:false,code:quotaCode,retryable:false,route:'/api/cron/social-prepare',delivery:delivery.delivery,message:'Upstash request quota is exhausted; this delivery is non-retryable.'})}return NextResponse.json({status:'failed',error:String(error?.message||error).slice(0,240)},{status:503,headers:{'Cache-Control':'no-store'}})}}export async function POST(request){return GET(request)}
