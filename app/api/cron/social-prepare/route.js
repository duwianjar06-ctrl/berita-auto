import {NextResponse} from 'next/server';import {prepareInstagramProductionQueue} from '../../../../lib/instagram-preparation-runtime.js';import {getInstagramAutomationSettings} from '../../../../lib/instagram-automation.js';import {kickInstagramPublisher} from '../../../../lib/instagram-auto-upload.js';import {withPersistenceSource,getJson,setJson} from '../../../../lib/persistence.js';import {classifyUpstashQuotaError,qstashDeliveryInfo,qstashNonRetryableResponse} from '../../../../lib/upstash-quota.js';import {invalidateInstagramAdminSnapshot} from '../../../../lib/instagram-admin-snapshot.js';import {cleanupLegacyInstagramBatch} from '../../../../lib/instagram-review-cleanup.js';import {SOCIAL_CARD_DESIGN_VERSION} from '../../../../lib/social-card-renderer.js';
export const dynamic='force-dynamic';export const runtime='nodejs';const CUTOVER_MARKER=`ba:social:instagram:design-cutover:${SOCIAL_CARD_DESIGN_VERSION}`;
function authorized(r){const s=process.env.CRON_SECRET||'';return Boolean(s&&(r.headers.get('authorization')||'')===`Bearer ${s}`)}
export async function GET(request){if(!authorized(request))return NextResponse.json({error:'Unauthorized'},{status:401});const delivery=qstashDeliveryInfo(request);console.log('[qstash-delivery]',JSON.stringify({route:'/api/cron/social-prepare',...delivery}));if(delivery.retried>0){console.log('[qstash-delivery-skip]',JSON.stringify({route:'/api/cron/social-prepare',scheduleId:delivery.scheduleId,messageId:delivery.messageId,retried:delivery.retried,reason:'qstash_retry_ignored_periodic_job'}));return NextResponse.json({status:'skipped',reason:'qstash_retry_ignored_periodic_job',delivery:delivery.delivery},{status:200,headers:{'Cache-Control':'no-store'}})}try{const merged=await withPersistenceSource('social-prepare',async()=>{let cleanup={status:'already_cutover',designVersion:SOCIAL_CARD_DESIGN_VERSION};if(!(await getJson(CUTOVER_MARKER))){const cutoverAt=new Date().toISOString();const result=await cleanupLegacyInstagramBatch({cutoverAt,dryRun:false,removedBy:'design-cutover',designVersion:SOCIAL_CARD_DESIGN_VERSION});await setJson(CUTOVER_MARKER,{designVersion:SOCIAL_CARD_DESIGN_VERSION,cutoverAt,removed:result.removed,candidates:result.candidates});cleanup={status:'completed',...result}}const result=await prepareInstagramProductionQueue({trigger:'social-prepare',now:Date.now()});const settings=await getInstagramAutomationSettings();let autoPublish=null;if(settings.autoUploadEnabled&&Number(result?.diagnostics?.readyCreated||0)>0)autoPublish=await kickInstagramPublisher();return{...result,legacyCleanup:cleanup,autoUploadEnabled:settings.autoUploadEnabled,autoPublishTriggered:Boolean(settings.autoUploadEnabled&&Number(result?.diagnostics?.readyCreated||0)>0),autoPublish}});invalidateInstagramAdminSnapshot();console.log('[instagram-prepare]',JSON.stringify(merged));return NextResponse.json(merged,{status:200,headers:{'Cache-Control':'no-store'}})}catch(error){invalidateInstagramAdminSnapshot();console.error('[instagram-prepare] failed',error);const quotaCode=classifyUpstashQuotaError(error);if(quotaCode)return qstashNonRetryableResponse({ok:false,code:quotaCode,retryable:false,route:'/api/cron/social-prepare',delivery:delivery.delivery,message:'Upstash request quota is exhausted; this delivery is non-retryable.'});return NextResponse.json({status:'failed',error:String(error?.message||error).slice(0,240)},{status:503,headers:{'Cache-Control':'no-store'}})}}
export async function POST(request){return GET(request)}
