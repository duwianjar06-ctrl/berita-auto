import {readArticles} from '../../../../lib/storage.js';
import {recordView} from '../../../../lib/analytics.js';

export const dynamic='force-dynamic';
const viewCount=async articleId=>{const url=process.env.UPSTASH_REDIS_REST_URL,token=process.env.UPSTASH_REDIS_REST_TOKEN;if(!url||!token)return 0;try{const response=await fetch(url,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify(['ZSCORE','ba:views:alltime',articleId]),cache:'no-store'});if(!response.ok)return 0;const data=await response.json();return Number(data.result)||0}catch{return 0}};
export async function POST(request){try{const body=await request.json();const articleId=String(body.articleId||'').trim();if(!/^[a-f0-9]{20,64}$/i.test(articleId))return Response.json({ok:false},{status:400});const articles=await readArticles();if(!articles.some(a=>a.id===articleId||a.fingerprint===articleId))return Response.json({ok:false},{status:404});const ua=request.headers.get('user-agent')||'';if(/bot|crawler|spider|slurp|headless|facebookexternalhit|googlebot|bingbot|ahrefs|semrush|uptime|monitor/i.test(ua))return Response.json({ok:true,counted:false,views:await viewCount(articleId)});const cookieName=`ba_view_${articleId.slice(0,32)}`;const cookies=request.headers.get('cookie')||'';if(new RegExp(`(?:^|;\\s*)${cookieName}=1(?:;|$)`).test(cookies))return Response.json({ok:true,counted:false,views:await viewCount(articleId)});await recordView({articleId,country:request.headers.get('x-vercel-ip-country'),region:request.headers.get('x-vercel-ip-country-region'),city:request.headers.get('x-vercel-ip-city')});const response=Response.json({ok:true,counted:true,views:await viewCount(articleId)});response.headers.set('Cache-Control','no-store');response.headers.append('Set-Cookie',`${cookieName}=1; Path=/; Max-Age=300; SameSite=Lax; Secure`);console.log('[ARTICLE_VIEW_INCREMENTED]',JSON.stringify({articleId}));return response}catch(error){console.error('[analytics:view]',error);return Response.json({ok:false},{status:503})}}
