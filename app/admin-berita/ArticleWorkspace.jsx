'use client';
import {useMemo,useState} from 'react';
const TYPES=['GUIDE','HOW_TO','TROUBLESHOOTING','EXPLAINER','COMPARISON','MAINTENANCE','FAQ','CHECKLIST','EVERGREEN'];
const esc=q=>`https://www.google.com/search?q=${encodeURIComponent(q||'')}`;
export default function ArticleWorkspace({initialArticles=[]}){const [articles,setArticles]=useState(initialArticles);const [sort,setSort]=useState('latest');const [filter,setFilter]=useState('');const [showCreate,setShowCreate]=useState(false);const [selected,setSelected]=useState(null);const rows=useMemo(()=>{let r=articles.filter(a=>!filter||a.status===filter||a.articleType===filter);if(sort==='score')r.sort((a,b)=>(b.searchScore||0)-(a.searchScore||0));else if(sort==='views')r.sort((a,b)=>(b.views||0)-(a.views||0));else r.sort((a,b)=>Date.parse(b.updatedAt||0)-Date.parse(a.updatedAt||0));return r},[articles,filter,sort]);const refresh=()=>fetch('/api/admin-berita/articles',{cache:'no-store'}).then(r=>r.ok?r.json():null).then(x=>x?.articles&&setArticles(x.articles)).catch(()=>{});return <section className="article-admin-workspace"><header className="admin-panel article-admin-head"><div><p className="admin-label">ARTIKEL</p><h2>Artikel Evergreen</h2><p className="muted">Panduan, tutorial, explainer dan konten evergreen berdasarkan kebutuhan pencarian pengguna.</p></div><button className="admin-primary-button" onClick={()=>setShowCreate(true)}>+ Buat Artikel</button></header><div className="article-admin-summary">{[['Published',articles.filter(a=>a.status==='PUBLISHED').length],['Draft',articles.filter(a=>a.status==='DRAFT').length],['Artikel Hari Ini',articles.filter(a=>new Date(a.createdAt||0).toDateString()===new Date().toDateString()).length],['Search High',articles.filter(a=>(a.searchScore||0)>=70).length],['Needs Review',articles.filter(a=>a.status==='NEEDS_REVIEW').length],['Total Artikel',articles.length]].map(([label,value])=><div key={label}><span>{label}</span><strong>{value}</strong></div>)}</div><section className="admin-panel article-admin-list"><div className="article-admin-filters"><select value={sort} onChange={e=>setSort(e.target.value)}><option value="latest">Terbaru</option><option value="score">Search Score Tertinggi</option><option value="views">Terpopuler</option></select><select value={filter} onChange={e=>setFilter(e.target.value)}><option value="">Semua</option><option value="PUBLISHED">Published</option><option value="DRAFT">Draft</option><option value="NEEDS_REVIEW">Needs Review</option>{TYPES.map(t=><option key={t} value={t}>{t}</option>)}</select></div>{rows.map(a=><article className="article-admin-card" key={a.id}><div className="article-admin-thumb">{a.imageUrl?<img src={a.imageUrl} alt=""/>:<span>BA</span>}</div><div className="article-admin-main"><div className="article-admin-top"><span>{a.articleType||'EVERGREEN'}</span><b>SEARCH {a.searchScore??'-'}</b><em>{a.status}</em></div><h3>{a.title}</h3><p>{a.category||'Umum'} · {a.updatedAt?new Date(a.updatedAt).toLocaleString('id-ID'):''}</p><div className="article-admin-query"><small>Target Google</small><strong>{a.primaryQuery||'-'}</strong></div><div className="article-admin-signals"><span>{a.searchIntent||'Informational'}</span><span>{a.articleQuality?.status||'Needs Review'}</span><span>{a.sourceCount||0} Referensi</span></div><div className="article-admin-actions"><a href={esc(a.primaryQuery)} target="_blank" rel="noopener noreferrer">Cek Google</a><a href={esc(a.articleSearchQuery||`${a.title||''} Berita Auto`)} target="_blank" rel="noopener noreferrer">Query Valid</a><a href={`/preview/artikel/${a.id}`} target="_blank" rel="noopener noreferrer">Preview</a><button onClick={()=>setSelected(a)}>Detail</button></div></div></article>)}{!rows.length&&<div className="empty-state">Belum ada Artikel.</div>}</section>{showCreate&&<CreateArticle onClose={()=>setShowCreate(false)} onSaved={()=>{setShowCreate(false);refresh()}}/>}{selected&&<ArticleDetail article={selected} onClose={()=>setSelected(null)}/>}</section>}
function CreateArticle({onClose,onSaved}){const [form,setForm]=useState({topic:'',title:'',category:'Automotive',articleType:'HOW_TO',primaryQuery:'',notes:'',status:'DRAFT'});const [saving,setSaving]=useState(false);const submit=async e=>{e.preventDefault();setSaving(true);try{const r=await fetch('/api/admin-berita/articles',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(form)});if(r.ok)onSaved();}finally{setSaving(false)}};return <div className="article-modal-backdrop"><form className="article-modal" onSubmit={submit}><h2>Buat Artikel</h2>{['title','topic','primaryQuery','notes'].map(k=><label key={k}>{k==='title'?'Title':k==='topic'?'Topic':k==='primaryQuery'?'Primary Query':'Notes'}<input value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<label>Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})}><option>Automotive</option><option>Technology</option><option>Public Utility</option><option>Finance</option><option>Education</option></select></label><label>Article Type<select value={form.articleType} onChange={e=>setForm({...form,articleType:e.target.value})}>{TYPES.map(t=><option key={t}>{t}</option>)}</select></label><div className="article-modal-actions"><button type="button" onClick={onClose}>Batal</button><button className="admin-primary-button" disabled={saving}>{saving?'Menyimpan…':'Simpan Draft'}</button></div></form></div>}
function ArticleDetail({article,onClose}){return <div className="article-modal-backdrop"><aside className="article-modal article-detail-modal"><button onClick={onClose}>Tutup</button><p className="admin-label">ARTICLE DETAIL</p><h2>{article.title}</h2><dl><dt>Status</dt><dd>{article.status}</dd><dt>Article Type</dt><dd>{article.articleType}</dd><dt>Search Score</dt><dd>{article.searchScore??'-'}</dd><dt>Article Quality</dt><dd>{article.articleQuality?.qualityScore??'-'}</dd><dt>Primary Query</dt><dd>{article.primaryQuery||'-'}</dd><dt>Secondary Queries</dt><dd>{(article.secondaryQueries||[]).join(' · ')||'-'}</dd><dt>Sources</dt><dd>{(article.sources||[]).map(s=>s.name||s.url).join(' · ')||'-'}</dd><dt>Canonical</dt><dd>{article.seo?.canonical||'-'}</dd><dt>Indexability</dt><dd>{article.robots||'noindex, nofollow'}</dd></dl></aside></div>}
