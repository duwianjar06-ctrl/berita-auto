'use client';
import {useMemo,useState} from 'react';
import {evaluateSearchOpportunity} from '../../lib/search-intent.js';
const meta=a=>evaluateSearchOpportunity(a);const reasonLabel=r=>({GOOGLE_SEARCH_OPPORTUNITY:'Google Opportunity',STRONG_SEARCH_INTENT:'Strong Search Intent',HIGH_PUBLIC_UTILITY:'Public Utility',BREAKING_NEWS:'Breaking News',FRESH_TOPIC:'Fresh',SCHEDULE_INFORMATION:'Schedule',PRICE_INFORMATION:'Price',RESULT_INFORMATION:'Result',POLICY_INFORMATION:'Policy',EVERGREEN_OPPORTUNITY:'Evergreen',UPDATE_EXISTING_ARTICLE:'Update Existing'}[r]||String(r||'').replaceAll('_',' '));const distributionLabel=d=>d==='WEBSITE_AND_INSTAGRAM'?'WEB + IG':d==='INSTAGRAM_PRIORITY'?'IG PRIORITY':d==='UPDATE_EXISTING'?'UPDATE':d==='SKIP'?'SKIP':'WEB';
export default function SearchIntentPanel({articles=[]}){const[sort,setSort]=useState('latest'),[filter,setFilter]=useState(''),[selected,setSelected]=useState(null),[csvStatus,setCsvStatus]=useState('');const rows=useMemo(()=>{let source=articles.slice(0,100);if(filter)source=source.filter(a=>{const m=meta(a);return filter==='high'?m.searchScore>=70:filter==='google'?a?.searchAnalytics?.source==='gsc_website'||a?.gscEvidence===true:filter==='ig'?m.socialScore>=70:filter==='trending'?m.searchSignals?.includes('fresh_topic'):filter==='utility'?m.searchSignals?.includes('high_utility'):filter==='low'?m.searchScore<50:filter==='skip'?m.distributionDecision==='SKIP':true});if(sort==='search')source.sort((a,b)=>meta(b).searchScore-meta(a).searchScore);else if(sort==='google')source.sort((a,b)=>Number(b?.searchAnalytics?.source==='gsc_website'||b?.gscEvidence===true)-Number(a?.searchAnalytics?.source==='gsc_website'||a?.gscEvidence===true)||meta(b).searchScore-meta(a).searchScore);else if(sort==='social')source.sort((a,b)=>meta(b).socialScore-meta(a).socialScore);else source.sort((a,b)=>Date.parse(b.publishedAt||b.sitePublishedAt||0)-Date.parse(a.publishedAt||a.sitePublishedAt||0));return source},[articles,filter,sort]);const summary=useMemo(()=>articles.reduce((acc,a)=>{const m=meta(a);acc.high+=m.searchScore>=70?1:0;acc.google+=(a?.searchAnalytics?.source==='gsc_website'||a?.gscEvidence===true)?1:0;acc.ig+=m.socialScore>=70?1:0;acc.trending+=m.searchSignals?.includes('fresh_topic')?1:0;acc.web+=m.distributionDecision==='WEBSITE_ONLY'?1:0;acc.skip+=m.distributionDecision==='SKIP'?1:0;return acc},{high:0,google:0,ig:0,trending:0,web:0,skip:0}),[articles]);const importCsv=async e=>{const file=e.target.files?.[0];if(!file)return;setCsvStatus('Mengimpor…');try{const text=await file.text(),r=await fetch('/api/admin-berita/search-data',{method:'POST',headers:{'content-type':'text/csv','x-source':'manual_import'},body:text}),j=await r.json();setCsvStatus(r.ok?`Imported ${j.imported||0} rows`:(j.error||'Import gagal'))}catch{setCsvStatus('Import gagal')}};return <section className="search-intent-admin admin-panel" aria-label="Search Opportunity"><div className="search-intent-head"><div><p className="admin-label">SEARCH OPPORTUNITY</p><h2>Search Opportunity</h2><p>Score internal dan evidence Google dipisahkan.</p></div><label className="search-csv-import">Search Console CSV<input type="file" accept=".csv,text/csv" onChange={importCsv}/></label></div><div className="search-summary search-summary-cards"><span><b>Search High</b><strong>{summary.high}</strong></span><span><b>Trending</b><strong>{summary.trending}</strong></span><span><b>IG Candidate</b><strong>{summary.ig}</strong></span><span><b>Website Only</b><strong>{summary.web}</strong></span><span><b>Google Opportunity</b><strong>{summary.google}</strong></span><span><b>Low Search Skipped</b><strong>{summary.skip}</strong></span></div><div className="admin-filters search-intent-filters"><select value={sort} onChange={e=>setSort(e.target.value)}><option value="latest">Terbaru</option><option value="search">Search Score Tertinggi</option><option value="google">Google Opportunity</option><option value="social">Social Opportunity</option></select><select value={filter} onChange={e=>setFilter(e.target.value)}><option value="">Semua Search</option><option value="high">Search High</option><option value="google">Google Opportunity</option><option value="ig">IG Opportunity</option><option value="trending">Trending</option><option value="utility">Public Utility</option><option value="low">Low Search</option><option value="skip">Skip Candidate</option></select><small>{csvStatus}</small></div><div className="search-opportunity-list">{rows.map(a=>{const m=meta(a);return <button key={a.id} className="search-opportunity-row" onClick={()=>setSelected(a)}><div className="search-score-pill"><span>SEARCH</span><strong>{m.searchScore}</strong><small>{m.searchPriority}</small></div><div className="search-row-main"><h3>{a.title}</h3><p>{a.category||'Tanpa Kategori'} · {a.sourceName||'Sumber publik'} · {a.publishedAt?new Date(a.publishedAt).toLocaleTimeString('id-ID',{hour:'2-digit',minute:'2-digit'}):'-'} WIB</p><div className="search-badges"><span>{reasonLabel(m.searchReason)}</span>{m.searchSignals?.[0]&&<span>{reasonLabel(m.searchSignals[0])}</span>}<span>{distributionLabel(m.distributionDecision)}</span></div><p className="search-query"><b>Query:</b> {m.primaryQuery}</p></div><div className="search-social">IG <b>{m.socialScore}</b></div></button>})}{!rows.length&&<div className="empty-state">Tidak ada candidate search sesuai filter.</div>}</div>{selected&&<aside className="search-detail-panel" role="dialog" aria-label="Search Opportunity detail"><button className="search-detail-close" onClick={()=>setSelected(null)}>Tutup</button>{(()=>{const m=meta(selected);return <><p className="admin-label">SEARCH OPPORTUNITY</p><h2>{selected.title}</h2><div className="search-detail-score"><strong>{m.searchScore}</strong><span>/ 100</span><b>{m.searchPriority}</b></div><div className="search-detail-grid"><span>Search Intent</span><strong>{m.searchIntent?.join(' · ')||'-'}</strong><span>Primary Query</span><strong>{m.primaryQuery}</strong><span>Secondary Queries</span><strong>{m.secondaryQueries?.join(' · ')||'-'}</strong><span>Why Selected</span><strong>{m.selectionReason?.map(reasonLabel).join(' · ')||'-'}</strong><span>Signals</span><strong>{m.searchSignals?.join(' · ')||'-'}</strong><span>Social Score</span><strong>{m.socialScore}</strong><span>Recommended Action</span><strong>{m.distributionDecision==='UPDATE_EXISTING'?'UPDATE':m.distributionDecision==='SKIP'?'SKIP':'PUBLISH'}</strong><span>Distribution</span><strong>{m.distributionDecision}</strong><span>Topic Cluster</span><strong>{m.topicCluster}</strong><span>Entities</span><strong>{Object.entries(m.entities||{}).flatMap(([k,v])=>v.map(x=>`${k}: ${x}`)).join(' · ')||'-'}</strong><span>Google Evidence</span><strong>{selected?.searchAnalytics?.source==='gsc_website'||selected?.gscEvidence===true?'YES':'No snapshot evidence'}</strong></div></>})()}</aside>}</section>}
