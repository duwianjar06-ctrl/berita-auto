import {readArticles} from './storage.js';
import {getJson,setJson,upsertIndexed,removeIndexed,acquireLock,releaseLock,listJson,persistenceConfigured} from './persistence.js';
import {readSocialQueue,buildEligibleSocialQueue,selectBestSocialArticle,readRecentPublished,markSocialPublished,deterministicCaption} from './social.js';
import {prepareInstagramCandidate,finalRevalidatePreparedCandidate} from './social-preparation.js';
import {instagramConfigured,getPublishingUsage,getMediaPermalink} from './instagram.js';
import {createAndPublishMedia} from '../worker/social-run.js';
import {telegramConfigured,sendTelegramMessage} from './telegram.js';
export const REVIEW_QUEUE_INDEX='ba:social:instagram:review:index';
export const REVIEW_QUEUE_TARGET=Math.max(1,Number(process.env.INSTAGRAM_READY_QUEUE_TARGET||5));
export const PREPARE_INTERVAL_MINUTES=Math.max(1,Number(process.env.INSTAGRAM_PREPARE_INTERVAL_MINUTES||1));
export const REVIEW_MODE=String(process.env.INSTAGRAM_PUBLISH_MODE||'review').trim().toLowerCase()==='auto'?'auto':'review';
const REMOVED_TTL_SECONDS=24*60*60;
const PUBLISH_LOCK_TTL_SECONDS=180;
const siteUrl=()=>String(process.env.SITE_URL||'https://berita-auto.vercel.app').replace(/\/$/,'');
const key=id=>`ba:social:instagram:review:item:${id}`;
const removedKey=id=>`ba:social:instagram:review:removed:${id}`;
function isReady(row){return row?.status==='READY'&&!row?.removedAt&&!row?.postedAt&&!row?.mediaId;}
export async function listInstagramReviewQueue(){if(!persistenceConfigured())return[];const rows=await listJson(REVIEW_QUEUE_INDEX);return rows.filter(row=>row.status!=='REMOVED').sort((a,b)=>Number(b.priorityScore||0)-Number(a.priorityScore||0)||String(b.readyAt||b.preparedAt||'').localeCompare(String(a.readyAt||a.preparedAt||'')));}
async function notifyNewReady(rows){if(!telegramConfigured()||!rows.length)return{sent:false,reason:'telegram_not_configured'};const fresh=[];for(const row of rows){if(!await getJson(`ba:social:instagram:review:notified:${row.queueId}`))fresh.push(row);}if(!fresh.length)return{sent:false,reason:'already_notified'};const text=`📸 <b>Berita Auto — Instagram</b>\n\n<b>${fresh.length} posting siap ditinjau.</b>\n\n${fresh.map(row=>`• ${String(row.title||'Berita').replace(/[<&>]/g,'')}`).join('\n')}\n\n<a href="${siteUrl()}/admin-instagram?view=ready">Buka Preview Instagram</a>`;const sent=await sendTelegramMessage(text);await Promise.all(fresh.map(row=>setJson(`ba:social:instagram:review:notified:${row.queueId}`,{queueId:row.queueId,notifiedAt:new Date().toISOString()})));return{sent:true,count:fresh.length,result:sent};}
export async function prepareInstagramReviewQueue({trigger='qstash',now=Date.now()}={}){if(REVIEW_MODE!=='review')return{status:'skipped',reason:'publish_mode_auto'};if(!instagramConfigured())return{status:'skipped',reason:'missing_configuration'};if(!persistenceConfigured())return{status:'skipped',reason:'persistence_not_configured'};const lockToken=`${trigger}:${now}:${Math.random().toString(36).slice(2,8)}`;if(!(await acquireLock('ba:social:instagram:prepare:lock',lockToken,120)))return{status:'skipped',reason:'lock_busy'};try{const existing=await listInstagramReviewQueue();const ready=existing.filter(isReady);if(ready.length>=REVIEW_QUEUE_TARGET)return{status:'ready',reason:'queue_full',ready:ready.length,target:REVIEW_QUEUE_TARGET,prepared:0,metaCalls:0};const articles=await readArticles();const queue=await readSocialQueue(100);const eligible=buildEligibleSocialQueue(queue.length?queue:articles.filter(a=>a?.id&&a?.sitePublishedAt).map(article=>({article,state:'queued'})));const recent=(await readRecentPublished(100)).map(item=>item.article||item).filter(Boolean);const ranked=[];for(const item of eligible.slice(0,20)){const article=item.article||item;const id=String(article?.id||item?.articleId||'');if(!id||await getJson(`ba:social:instagram:published:${id}`)||await getJson(removedKey(id)))continue;if(existing.some(row=>String(row.articleId)===id&&['READY','PREPARING','PUBLISHING','POSTED'].includes(row.status)))continue;ranked.push({...item,selectionScore:selectBestSocialArticle([item],{now,recentPublished:recent})?.selectionScore??0});}ranked.sort((a,b)=>Number(b.selectionScore||0)-Number(a.selectionScore||0));const picked=ranked.slice(0,Math.min(5,Math.max(0,REVIEW_QUEUE_TARGET-ready.length)));const prepared=[];for(let i=0;i<picked.length;i++){const item=picked[i];const article=item.article||item;const queueId=`${article.id}:${article.stableId||article.id}`;const preparedAt=new Date().toISOString();const base={queueId,articleId:article.id,stableId:article.stableId||article.id,title:article.title,category:article.category,publisher:article.publisher||article.sourceName||'Sumber publik',sourceUrl:article.sourceUrl||article.source||article.originalUrl||null,canonicalUrl:article.canonicalUrl||`${siteUrl()}/berita/${encodeURIComponent(article.slug||article.id)}`,sourcePublishedAt:article.sourcePublishedAt||article.publishedAt||null,sitePublishedAt:article.sitePublishedAt||null,status:'PREPARING',preparedAt,priorityScore:item.selectionScore||0};await upsertIndexed(REVIEW_QUEUE_INDEX,key(queueId),base);const result=await prepareInstagramCandidate(item,{siteUrl:siteUrl(),runId:`review-${now}-${i+1}`,full:i===0||!item.cardUrls?.length});const caption=deterministicCaption(article,siteUrl());const row={...base,sourceImageUrl:result.image?.imageUrl||null,imageOriginUrl:result.image?.imageOriginUrl||null,imageValidation:result.image||null,imageValidationStatus:result.imageValidationStatus||result.image?.status||'UNKNOWN',imageRelationship:result.imageRelationship||result.image?.status||'UNKNOWN',cardUrls:result.cardUrls||[],previewUrl:result.previewUrl||result.cardUrls?.[0]||null,cardChecks:result.cardChecks||[],caption,captionOriginal:caption,captionEdited:false,captionStatus:'READY',dedupeStatus:'NOT_POSTED',status:result.status==='READY'?'READY':'FAILED',preparationState:result.preparationState||result.status,preparedAt:result.preparedAt||preparedAt,readyAt:result.readyAt||null,render:result.render||null,font:result.font||null,glyph:result.glyph||null,lastError:result.status==='READY'?null:result.reason||'PREPARATION_FAILED',updatedAt:new Date().toISOString()};await upsertIndexed(REVIEW_QUEUE_INDEX,key(queueId),row);prepared.push(row);}const newlyReady=prepared.filter(isReady);const notification=await notifyNewReady(newlyReady);return{status:'prepared',ready:ready.length+newlyReady.length,preparing:prepared.filter(x=>x.status==='PREPARING').length,failed:prepared.filter(x=>x.status==='FAILED').length,target:REVIEW_QUEUE_TARGET,prepared:prepared.length,newlyReady:newlyReady.length,notification,metaCalls:0,trigger};}finally{await releaseLock('ba:social:instagram:prepare:lock',lockToken).catch(()=>{});}}
export async function getInstagramReviewSnapshot(){const rows=await listInstagramReviewQueue();const ready=rows.filter(isReady);return{mode:REVIEW_MODE,prepareIntervalMinutes:PREPARE_INTERVAL_MINUTES,readyQueueTarget:REVIEW_QUEUE_TARGET,items:rows,ready,counts:{ready:ready.length,preparing:rows.filter(x=>x.status==='PREPARING').length,failed:rows.filter(x=>x.status==='FAILED').length,posted:rows.filter(x=>x.status==='POSTED').length,removed:rows.filter(x=>x.status==='REMOVED').length},refreshedAt:new Date().toISOString()};}
export async function editInstagramReviewCaption(queueId,caption){const row=await getJson(key(queueId));if(!row||!['READY','FAILED'].includes(row.status))throw new Error('review_item_not_editable');const value=String(caption??'').trim();if(!value)throw new Error('caption_required');const updated={...row,caption:value,captionEdited:true,captionStatus:'READY',updatedAt:new Date().toISOString()};await setJson(key(queueId),updated);return updated;}
export async function removeInstagramReviewItem(queueId){const row=await getJson(key(queueId));if(!row)throw new Error('review_item_not_found');const updated={...row,status:'REMOVED',removedAt:new Date().toISOString(),updatedAt:new Date().toISOString()};await setJson(key(queueId),updated);await setJson(removedKey(row.articleId),{articleId:row.articleId,removedAt:updated.removedAt,expiresAt:new Date(Date.now()+REMOVED_TTL_SECONDS*1000).toISOString()});await removeIndexed(REVIEW_QUEUE_INDEX,key(queueId));return updated;}
export async function publishInstagramReviewItem(queueId,{idempotencyKey}={}){if(REVIEW_MODE!=='review')throw new Error('review_mode_not_enabled');if(!instagramConfigured())throw new Error('instagram_not_configured');const row=await getJson(key(queueId));if(!row||!isReady(row))throw new Error('review_item_not_ready');const lockKey=`ba:social:instagram:publish:${queueId}`;const token=`${idempotencyKey||Date.now()}:${Math.random().toString(36).slice(2,8)}`;if(!(await acquireLock(lockKey,token,PUBLISH_LOCK_TTL_SECONDS)))throw new Error('publish_already_in_progress');try{const current=await getJson(key(queueId));if(!current||!isReady(current))throw new Error('review_item_not_ready');const article=(await readArticles()).find(item=>String(item.id)===String(current.articleId));if(!article)throw new Error('article_not_found');const final=await finalRevalidatePreparedCandidate({...current,article},{siteUrl:siteUrl(),now:Date.now()});if(!final.valid)throw new Error(`final_validation_failed:${final.reason}`);const usage=await getPublishingUsage();if(usage?.available===false){await setJson(key(queueId),{...current,lastError:'META_LIMITED',updatedAt:new Date().toISOString()});throw new Error('META_LIMITED');}const publishing={...current,status:'PUBLISHING',publishingStartedAt:new Date().toISOString(),updatedAt:new Date().toISOString()};await setJson(key(queueId),publishing);const result=await createAndPublishMedia(article,publishing.caption||publishing.captionOriginal||'',publishing.cardUrls);if(!result.ready?.ready){await setJson(key(queueId),{...publishing,status:'FAILED',lastError:'META_CONTAINER_NOT_READY',retryable:true,updatedAt:new Date().toISOString()});throw new Error('META_CONTAINER_NOT_READY');}const mediaId=result.mediaId;let permalink=null;try{permalink=await getMediaPermalink(mediaId);}catch{}const publishedAt=new Date().toISOString();let record=null;for(let attempt=1;attempt<=3;attempt++){try{record=await markSocialPublished({...publishing,article},{mediaId,mediaUrl:permalink,publishedAt,publishMode:result.publishMode||(result.carousel?'carousel':'single-image')});break;}catch(error){if(attempt<3)await new Promise(resolve=>setTimeout(resolve,500*attempt));}}if(!record)throw new Error('history_persistence_failed');const posted={...publishing,status:'POSTED',mediaId,permalink,publishedAt,publishMode:result.publishMode||(result.carousel?'carousel':'single-image'),containerId:result.containerId||null,childContainerIds:result.childIds||[],fallbackFromCarousel:Boolean(result.fallbackFromCarousel),postedAt,historyPersisted:true,updatedAt:new Date().toISOString()};await setJson(key(queueId),posted);return posted;}catch(error){const current=await getJson(key(queueId));const limited=Number(error?.metaCode||0)===9&&Number(error?.metaSubcode||0)===2207042;if(current&&current.status==='PUBLISHING')await setJson(key(queueId),{...current,status:limited?'READY':'FAILED',lastError:limited?'META_LIMITED':String(error?.message||error).slice(0,240),updatedAt:new Date().toISOString()});throw error;}finally{await releaseLock(lockKey,token).catch(()=>{});}}
