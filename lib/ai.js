export async function generateArticle(item){const key=process.env.OPENAI_API_KEY;if(!key)return {title:item.title,content:`${item.summary||item.title}\n\nSumber: ${item.url}`};const prompt=`Tulis artikel berita orisinal berbahasa Indonesia berdasarkan fakta berikut. Jangan mengarang fakta. Buat judul menarik dan isi 5 paragraf. Sumber: ${item.url}\nJudul: ${item.title}\nRingkasan: ${item.summary}`;const res=await fetch('https://api.openai.com/v1/chat/completions',{method:'POST',headers:{'content-type':'application/json',authorization:`Bearer ${key}`},body:JSON.stringify({model:process.env.OPENAI_MODEL||'gpt-4o-mini',messages:[{role:'user',content:prompt}],temperature:.4})});if(!res.ok)throw new Error(`AI request failed: ${res.status}`);const data=await res.json();return {title:item.title,content:data.choices?.[0]?.message?.content?.trim()||item.summary||item.title};}
