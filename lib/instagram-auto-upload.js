import {getJson,setJson,acquireLock,releaseLock,upsertIndexed} from './persistence.js';import {getInstagramAutomationSettings} from './instagram-automation.js';const AUTO_LOCK='ba:social:instagram:auto-upload:lock',LAST_PUBLISH_KEY='ba:social:instagram:auto-upload:last-publish',ITEM=id=>`ba:social:instagram:review:item:${id}`;const isReady=row=>row?.status==='READY'&&!row?.removedAt&&!row?.postedAt&&!row?.mediaId;
export async function runInstagramAutoUpload({trigger='scheduled',now=Date.now()}={}){const settings=await getInstagramAutomationSettings(),base={autoUploadEnabled:settings.autoUploadEnabled,autoPublishTriggered:true,readyFound:0,eligibleReady:0,publishAttempted:0,publishPosted:0,publishFailed:0,remainingReady:0,rateLimited:false,metaCalls:0};if(!settings.autoUploadEnabled)return{status:'skipped',reason:'auto_upload_off',...base,autoPublishTriggered:false};const token=`${trigger}:${now}:${Math.random().toString(36).slice(2,8)}`;if(!(await acquireLock(AUTO_LOCK,token,180)))return{status:'skipped',reason:'auto_upload_lock_busy',...base,autoPublishTriggered:false};const start=Date.now();try{if(trigger!=='scheduled')process.env.INSTAGRAM_PUBLISH_MODE='review';const review=await import('./instagram-review.js');const {listInstagramReviewQueue,publishInstagramReviewItem,REVIEW_QUEUE_INDEX}=review;const rows=await listInstagramReviewQueue();base.readyFound=rows.filter(isReady).length;const immediate=/immediate|prepare|kick/i.test(trigger),last=await getJson(LAST_PUBLISH_KEY),lastAt=Date.parse(last?.publishedAt||''),interval=settings.autoUploadIntervalMinutes*60000;if(!immediate&&Number.isFinite(lastAt)&&now-lastAt<interval)return{status:'skipped',reason:'interval_backoff',nextAt:new Date(lastAt+interval).toISOString(),...base,eligibleReady:base.readyFound,durationMs:Date.now()-start};const eligible=[];for(const row of rows){if(!isReady(row))continue;const current=await getJson(ITEM(row.queueId));if(isReady(current))eligible.push(current)}base.eligibleReady=eligible.length;for(const row of eligible){if(!isReady(await getJson(ITEM(row.queueId))))continue;base.publishAttempted++;try{const posted=await publishInstagramReviewItem(row.queueId,{idempotencyKey:`auto:${row.queueId}:${now}`});base.publishPosted++;await upsertIndexed(REVIEW_QUEUE_INDEX,ITEM(row.queueId),posted);await setJson(LAST_PUBLISH_KEY,{queueId:posted.queueId,publishedAt:posted.publishedAt||new Date().toISOString()});if(!immediate)break}catch(e){const msg=String(e?.message||e);const limited=msg==='META_LIMITED'||(/meta/i.test(msg)&&/limit|rate/i.test(msg));if(limited){base.rateLimited=true;break}base.publishFailed++;const latest=await getJson(ITEM(row.queueId));if(latest)await upsertIndexed(REVIEW_QUEUE_INDEX,ITEM(row.queueId),latest)}}base.remainingReady=(await listInstagramReviewQueue()).filter(isReady).length;if(base.rateLimited)return{status:'partial',reason:'meta_rate_limited',...base,durationMs:Date.now()-start};if(base.publishPosted)return{status:base.remainingReady?'partial':'published',...base,durationMs:Date.now()-start};return{status:'skipped',reason:base.eligibleReady?'publish_failed_or_stale':'no_ready_item',...base,durationMs:Date.now()-start}}finally{await releaseLock(AUTO_LOCK,token).catch(()=>{})}}
export async function kickInstagramPublisher(){return runInstagramAutoUpload({trigger:'prepare-immediate',now:Date.now()})}
