import {evaluateSearchOpportunity} from './search-intent.js';
import {listAllArticles,findDuplicateArticle} from './article-storage.js';
import {readSearchAnalytics} from './search-data.js';
const TYPE_RULES=[['HOW_TO',/\b(cara|bagaimana|langkah|menyetel|memilih|mengecek|membaca)\b/i],['TROUBLESHOOTING',/\b(penyebab|kenapa|brebet|mati|rusak|soak|tidak stabil|overheat)\b/i],['COMPARISON',/\b(perbedaan|vs|dibandingkan|bagus mana|lebih baik)\b/i],['FAQ',/\b(apa itu|apakah|faq|pertanyaan)\b/i],['CHECKLIST',/\b(tanda-tanda|cek|daftar)\b/i],['MAINTENANCE',/\b(perawatan|merawat|servis|maintenance)\b/i],['EXPLAINER',/\b(mengapa|fungsi|penjelasan|arti)\b/i]];
const STOP=new Set('yang dan untuk dari dengan pada dalam ini itu agar atau akan telah dapat adalah karena juga tidak lebih para sebuah motor mobil berita terbaru hari ini langsung cara tips panduan'.split(' '));
const EVERGREEN_FALLBACK=[
  ['Cara menyetel karburator motor agar langsam stabil','Automotive'],['Penyebab motor brebet saat digas dan cara mengeceknya','Automotive'],['Cara membaca warna busi motor','Automotive'],['Penyebab speedometer motor mati','Automotive'],['Cara mengecek aki motor mulai soak','Automotive'],['Cara memilih oli motor yang tepat','Automotive'],['Tanda-tanda kampas rem harus diganti','Automotive'],['Penyebab mesin motor cepat panas','Automotive'],['Cara merawat motor setelah terendam banjir','Automotive'],['Perbedaan motor injeksi dan karburator','Automotive'],['Cara merawat baterai ponsel agar lebih awet','Technology'],['Cara menghemat listrik rumah tanpa mengurangi kenyamanan','Public Utility']
];
export function inferArticleType(topic=''){for(const [type,re] of TYPE_RULES)if(re.test(topic))return type;return 'EVERGREEN'}
export function normalizeTopic(topic=''){return String(topic||'').replace(/\s+/g,' ').trim()}
export function primaryIntentForTopic(topic=''){const t=normalizeTopic(topic).toLowerCase();if(/\b(cara|bagaimana|menyetel|mengecek|memilih|merawat)\b/.test(t))return t;if(/\b(penyebab|kenapa|tanda)\b/.test(t))return t;if(/\b(perbedaan|vs|bagus mana)\b/.test(t))return t;return t}
export function buildArticleIdea(topic,{category='Automotive',articleType}={}){const clean=normalizeTopic(topic).replace(/^panduan\s+/i,'');return{contentType:'article',title:clean,topic:clean,topicCluster:clean.toLowerCase().replace(/[^a-z0-9\s]/g,'').replace(/\s+/g,' ').trim(),category,articleType:articleType||inferArticleType(clean),primaryQuery:primaryIntentForTopic(clean),searchIntent:inferArticleType(clean)==='TROUBLESHOOTING'?'problem_solution':'informational',status:'IDEA'}}
export async function findArticleIdeas({topic='',limit=10}={}){if(topic)return[buildArticleIdea(topic)];return discoverArticleOpportunities({limit});}
function candidateFromQuery(query,category='Automotive'){const clean=normalizeTopic(query).replace(/^panduan\s+/i,'');if(clean.length<12||!/\b(cara|penyebab|kenapa|tanda|fungsi|perbedaan|bagaimana|memilih|mengecek|merawat|tips|solusi|berapa|kapan)\b/i.test(clean))return null;return buildArticleIdea(clean,{category})}
export async function discoverArticleOpportunities({limit=10}={}){const rows=await listAllArticles();const analytics=await readSearchAnalytics().catch(()=>[]);const candidates=[];const seen=new Set();const add=async(idea,sourceScore=0,reason='evergreen')=>{if(!idea||seen.has(idea.primaryQuery))return;const duplicate=await findDuplicateArticle(idea);if(duplicate)return;seen.add(idea.primaryQuery);candidates.push({...idea,sourceScore,opportunityReason:reason});};
  const queryRows=[...analytics].filter(x=>String(x.query||'').trim()).sort((a,b)=>(Number(b.impressions||0)*2+Number(b.clicks||0))-(Number(a.impressions||0)*2+Number(a.clicks||0)));
  for(const row of queryRows){const idea=candidateFromQuery(row.query);if(idea)await add(idea,Math.min(100,Number(row.impressions||0)+Number(row.clicks||0)*5),'search_console');if(candidates.length>=limit)break;}
  for(const n of rows.slice(0,100)){const m=evaluateSearchOpportunity(n);if(Number(m.searchScore||0)<70)continue;const sourceTopic=String(n.topicCluster||'').trim();if(!sourceTopic)continue;const idea=candidateFromQuery(sourceTopic,n.category||'Automotive')||candidateFromQuery(`cara ${sourceTopic}`,n.category||'Automotive');if(idea)await add(idea,Number(m.searchScore||0),'news_cluster');if(candidates.length>=limit)break;}
  for(const [topic,category] of EVERGREEN_FALLBACK){const idea=buildArticleIdea(topic,{category});await add(idea,70,'evergreen_library');if(candidates.length>=limit)break;}
  return candidates.sort((a,b)=>(Number(b.sourceScore||0)+scoreArticleIdea(b))-(Number(a.sourceScore||0)+scoreArticleIdea(a))).slice(0,limit);
}
export async function discoverArticleOpportunity(){const rows=await discoverArticleOpportunities({limit:10});return rows[0]||null}
export function scoreArticleIdea(idea={}){const q=String(idea.primaryQuery||'').trim();return Math.max(0,Math.min(100,40+(q.split(/\s+/).length>=3?20:0)+(idea.articleType!=='EVERGREEN'?15:5)+(idea.category?10:0)+(q.length>=20?15:0)))}
