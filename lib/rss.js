import {sources} from './sources.js';
function clean(value=''){return value.replace(/<!\[CDATA\[([\s\S]*?)\]\]>/g,'$1').replace(/<[^>]*>/g,' ').replace(/&amp;/g,'&').replace(/&quot;/g,'"').replace(/&#39;/g,"'").replace(/&lt;/g,'<').replace(/&gt;/g,'>').replace(/&#(\d+);/g,(_,n)=>String.fromCharCode(Number(n))).replace(/\s+/g,' ').trim();}
function tag(xml,name){const m=xml.match(new RegExp(`<${name}[^>]*>([\\s\\S]*?)</${name}>`,'i'));return m?clean(m[1]):'';}
export async function fetchNews(){const out=[];let checked=0;for(const entry of sources){const source=typeof entry==='string'?{name:entry,url:entry}:entry;checked++;try{console.log(`[rss] fetching ${source.name} ${source.url}`);const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),15000);const res=await fetch(source.url,{signal:controller.signal,headers:{'user-agent':'BeritaAuto/1.0'}});clearTimeout(timer);if(!res.ok){console.error(`[rss] ${res.status} ${source.url}`);continue;}const xml=await res.text();const items=xml.match(/<item>[\s\S]*?<\/item>/gi)||[];console.log(`[rss] ${source.name}: ${items.length} items`);for(const item of items){const title=tag(item,'title');const link=tag(item,'link');if(title&&link)out.push({title,summary:tag(item,'description'),url:link,publishedAt:tag(item,'pubDate'),sourceUrl:source.url,sourceName:source.name,category:source.category||''});}}catch(error){console.error(`[rss] source failed ${source.name}: ${error.message}`);}}console.log(`[rss] sources checked: ${checked}`);console.log(`[rss] items fetched: ${out.length}`);return out;}
