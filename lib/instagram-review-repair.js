import {readArticles} from './storage.js';
import {listJson,getJson,setJson,upsertIndexed,removeIndexed,setRemove} from './persistence.js';
import {prepareInstagramCandidate} from './social-preparation.js';
import {articlePath,articleStableId} from './article-url.js';
import {deterministicCaption} from './social.js';

const INDEX='ba:social:instagram:review:index';
const KEY=id=>`ba:social:instagram:review:item:${id}`;
const PUBLISHED_KEY=id=>`ba:social:instagram:published:${id}`;
const PUBLISH_QUEUE_INDEX='ba:social:instagram:publish-queue:index';
const MAX_REPAIRS=5;
const RETRY_DELAYS_MINUTES=[5,15,30];
const CANONICAL_TIMEOUT_MS=3000;
const siteUrl=()=>String(process.env.SITE_URL||'https://berita-auto.vercel.app').replace(/\/$/,'');
function classify(result){const code=String(result?.articleCheck?.failureCode||'').toUpperCase();if(code)return code;const reason=String(result?.reason||'').toUpperCase();if(reason==='CARD_UPLOAD_FAILED')return'CARD_UPLOAD_FAILED';if(result?.cardFailureCode)return String(result.cardFailureCode).toUpperCase();if(reason==='CARD_PUBLIC_CHECK_FAILED'||reason==='CARD_VALIDATION_FAILED')return'CARD_PUBLIC_CHECK_FAILED';if(reason==='CAPTION_FAILED')return'CAPTION_FAILED';if(reason==='IMAGE_PROVENANCE_INVALID')return'IMAGE_PROVENANCE_INVALID';if(result?.image?.status==='FALLBACK_USED'&&reason)return'SOURCE_IMAGE_MISSING';if(reason==='ARTICLE_INVALID')return'UNKNOWN_ARTICLE_INVALID';return reason||'UNKNOWN_ARTICLE_INVALID';}
function human(code){const map={ARTICLE_NOT_FOUND:'Artikel tidak ditemukan di database Berita Auto.',ARTICLE_NOT_PUBLISHED:'Artikel belum berstatus publik.',ARTICLE_MISSING_ID:'Artikel tidak memiliki ID stabil.',ARTICLE_MISSING_STABLE_ID:'Artikel tidak memiliki stable ID untuk canonical route.',ARTICLE_MISSING_TITLE:'Judul artikel tidak tersedia.',CANONICAL_URL_MISSING:'Canonical URL tidak dapat dibangun dari route artikel resmi.',CANONICAL_URL_BUILD_FAILED:'Canonical URL gagal dibangun dari route artikel resmi.',CANONICAL_URL_404:'URL Berita Auto menghasilkan HTTP 404.',CANONICAL_URL_5XX:'URL Berita Auto menghasilkan server error.',CANONICAL_URL_TIMEOUT:'Pengecekan halaman berita melewati batas waktu.',CANONICAL_URL_UNREACHABLE:'URL canonical tidak dapat diakses saat validasi.',CANONICAL_REDIRECT_INVALID:'Canonical URL mengarah ke route internal yang tidak valid.',SOURCE_URL_INVALID:'URL sumber artikel tidak valid.',SOURCE_URL_FAILED:'URL sumber artikel gagal divalidasi.',SOURCE_IMAGE_MISSING:'Foto sumber tidak tersedia; fallback Berita Auto digunakan.',CARD_UPLOAD_FAILED:'Card Instagram gagal disimpan.',CARD_PUBLIC_CHECK_FAILED:'Card Instagram gagal divalidasi secara publik.',CARD_PUBLIC_TIMEOUT:'Card sudah dibuat tetapi URL publik belum merespons tepat waktu.',CARD_PUBLIC_404:'Card sudah dibuat tetapi URL publik menghasilkan HTTP 404.',CARD_PUBLIC_5XX:'Card sudah dibuat tetapi URL publik menghasilkan server error.',CARD_PUBLIC_CONTENT_TYPE_INVALID:'URL publik card mengembalikan content type yang tidak valid.',CARD_PUBLIC_EMPTY:'URL publik card mengembalikan data kosong.',CARD_PUBLIC_DECODE_FAILED:'Card publik gagal didecode atau dimensinya tidak valid.',CAPTION_FAILED:'Caption gagal disiapkan.',IMAGE_PROVENANCE_INVALID:'Asal gambar tidak dapat divalidasi.',UNKNOWN_ARTICLE_INVALID:'Validasi artikel gagal dan membutuhkan pemeriksaan lebih lanjut.'};return map[code]||code;}
function repairable(row,now=Date.now()){if(!row||!['FAILED','ATTENTION'].includes(String(row.status||'').toUpperCase())||row.removedAt||row.postedAt)return false;const next=Date.parse(row.nextRetryAt||'');return!Number.isFinite(next)||next<=now;}
function recoverable(code){return code==='CANONICAL_URL_TIMEOUT'||code==='CANONICAL_URL_UNREACHABLE'||code.startsWith('CARD_PUBLIC_')||code==='CARD_PUBLIC_CHECK_FAILED'||code==='CARD_UPLOAD_FAILED'||code==='STORAGE_FAILED';}
function retryDelayMs(attemptCount){const index=Math.max(0,Math.min(RETRY_DELAYS_MINUTES.length-1,Number(attemptCount||1)-1));return RETRY_DELAYS_MINUTES[index]*60*1000;}
async function persist(row,patch){const updated={...row,...patch,updatedAt:new Date().toISOString()};await upsertIndexed(INDEX,KEY(updated.queueId),updated);return updated;}
async function reconcilePublishedRow(row,{now=Date.now()}={}){const status=String(row?.status||'').toUpperCase();if(!['PUBLISHING','PUBLISH_AMBIGUOUS'].includes(status)||row?.postedAt||row?.mediaId&&status==='POSTED')return null;const marker=await getJson(PUBLISHED_KEY(row.articleId));if(!marker?.mediaId)return null;const publishedAt=marker.publishedAt||marker.updatedAt||row.publishedAt||new Date(now).toISOString();const permalink=marker.externalPostUrl||marker.externalPostPermalink||marker.mediaUrl||marker.permalink||row.permalink||null;const posted={...row,status:'POSTED',state:'published',dedupeStatus:'POSTED',mediaId:String(marker.mediaId),externalPostId:String(marker.externalPostId||marker.mediaId),externalPostUrl:permalink,permalink,publishedAt,postedAt:publishedAt,historyPersisted:true,currentStage:'SUCCESS',lastError:null,retryReason:null,nextRetryAt:null,updatedAt:new Date().toISOString()};await setJson(KEY(row.queueId),posted);await removeIndexed(INDEX,KEY(row.queueId));await setRemove(PUBLISH_QUEUE_INDEX,KEY(row.queueId));return{queueId:row.queueId,articleId:row.articleId,mediaId:String(marker.mediaId),publishedAt,permalink,statusBefore:status,statusAfter:'POSTED'};}
async function fetchCanonicalBounded(url){const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),CANONICAL_TIMEOUT_MS);const started=Date.now();try{const response=await fetch(url,{method:'GET',headers:{Accept:'text/html'},cache:'no-store',redirect:'follow',signal:controller.signal});return{status:response.status,durationMs:Date.now()-started,timeout:false};}catch(error){return{status:null,durationMs:Date.now()-started,timeout:error?.name==='AbortError',error:String(error?.message||error).slice(0,160)};}finally{clearTimeout(timer);}}

export async function repairInstagramReviewItem(queueId,{now=Date.now()}={}){const row=await getJson(KEY(String(queueId)));if(!row)return{status:'not_found',queueId:String(queueId)};const published=reconcilePublishedRow(row,{now});if(published)return{status:'already_posted_reconciled',queueId:String(queueId),reconciled:published,metaCalls:0};if(!repairable(row,now))return{status:'skipped',queueId:String(queueId),reason:'not_due',item:row};const articles=await readArticles();const article=articles.find(item=>String(item?.id)===String(row.articleId));const originalFailure={failureCode:row.failureCode||row.attentionReason||row.lastError||'ARTICLE_INVALID',failureStage:row.failureStage||null,failureMessage:row.failureMessage||row.lastError||null,failedAt:row.failedAt||row.updatedAt||null,attemptedCanonicalUrl:row.attemptedCanonicalUrl||row.canonicalUrl||null,canonicalHttpStatus:row.canonicalHttpStatus||null,failedSlide:row.failedSlide||null};if(!article){const updated=await persist(row,{status:'FAILED',attentionReason:'ARTICLE_NOT_FOUND',failureCode:'ARTICLE_NOT_FOUND',failureStage:'ARTICLE_LOOKUP',failureMessage:human('ARTICLE_NOT_FOUND'),articleFound:false,articlePublished:false,repairAttempted:true,repairResult:'FAILED',failedAt:new Date().toISOString(),nextRetryAt:null});return{status:'failed',queueId:row.queueId,recovered:false,item:updated};}const stableId=articleStableId(article);const canonicalUrl=stableId&&articlePath(article)?`${siteUrl()}${articlePath(article)}`:'';let canonicalHttpStatus=null;if(canonicalUrl){const checked=await fetchCanonicalBounded(canonicalUrl);canonicalHttpStatus=checked.status;}const priorCardFailure=String(row.failureCode||row.attentionReason||'').toUpperCase();const isStaleCardFailure=priorCardFailure==='CARD_PUBLIC_CHECK_FAILED'||priorCardFailure.startsWith('CARD_PUBLIC_');const candidateInput={article};if(isStaleCardFailure&&Array.isArray(row.cardUrls)&&row.cardUrls.length){candidateInput.cardUrls=row.cardUrls;candidateInput.previewUrl=row.previewUrl||row.cardUrls[0]||null;}const result=await prepareInstagramCandidate(candidateInput,{siteUrl:siteUrl(),runId:`repair-${Date.now()}-${row.queueId}`,full:true});const failureCode=classify(result);const failedSlideIndex=Array.isArray(result.cardChecks)?result.cardChecks.findIndex(item=>item?.failureCode||item?.valid===false):-1;const failedSlide=failedSlideIndex>=0?failedSlideIndex+1:null;if(result.status==='READY'){const history=Array.isArray(row.repairHistory)?row.repairHistory:[];const resolvedFailure={...originalFailure,resolvedAt:new Date().toISOString(),repairResult:'SUCCESS',currentCanonicalUrl:canonicalUrl,canonicalHttpStatus:canonicalHttpStatus||200};const caption=row.captionEdited?row.caption:deterministicCaption(article,siteUrl());const updated=await persist(row,{title:article.title,category:article.category,articleId:article.id,stableId:stableId||row.stableId,canonicalUrl,attemptedCanonicalUrl:canonicalUrl,canonicalHttpStatus:canonicalHttpStatus||200,sourceUrl:article.sourceUrl||article.source||article.originalUrl||row.sourceUrl||null,sourceImageUrl:result.image?.imageUrl||null,imageOriginUrl:result.image?.imageOriginUrl||null,imageValidation:result.image||null,imageValidationStatus:result.image?.status||'UNKNOWN',imageRelationship:result.imageRelationship||result.image?.status||'UNKNOWN',cardUrls:result.cardUrls||[],previewUrl:result.previewUrl||result.cardUrls?.[0]||null,cardChecks:result.cardChecks||[],render:result.render||null,font:result.font||null,glyph:result.glyph||null,caption,captionOriginal:row.captionOriginal||deterministicCaption(article,siteUrl()),captionEdited:Boolean(row.captionEdited),captionEditedAt:row.captionEditedAt||null,captionStatus:'READY',status:'READY',preparationState:'READY',readyAt:result.readyAt||new Date().toISOString(),lastError:null,attentionReason:null,failureCode:null,failureStage:null,failureMessage:null,failureDetail:null,failedSlide:null,failedAt:null,repairAttempted:true,repairResult:'SUCCESS',resolvedFailure,repairHistory:[...history,resolvedFailure].slice(-10),articleFound:true,articlePublished:Boolean(article.sitePublishedAt),originalFailure:row.originalFailure||originalFailure,nextRetryAt:null,attemptCount:0});return{status:'recovered',queueId:row.queueId,recovered:true,item:updated,oldFailure:originalFailure};}const attemptCount=Number(row.attemptCount||0)+1;const nextRetryAt=recoverable(failureCode)?new Date(now+retryDelayMs(attemptCount)).toISOString():null;const updated=await persist(row,{title:article.title,category:article.category,stableId:stableId||row.stableId,canonicalUrl:canonicalUrl||row.canonicalUrl,attemptedCanonicalUrl:result.articleCheck?.canonicalUrl||canonicalUrl||row.attemptedCanonicalUrl||null,canonicalHttpStatus:result.articleCheck?.publicStatus==='PASS'?200:(canonicalHttpStatus||null),articleFound:true,articlePublished:Boolean(article.sitePublishedAt),status:'FAILED',attentionReason:failureCode,failureCode,failureStage:result.cardFailureStage||result.articleCheck?.failureStage||'PREPARATION',failureMessage:human(failureCode),lastError:failureCode,failedSlide,repairAttempted:true,repairResult:'FAILED',failedAt:new Date().toISOString(),attemptCount,nextRetryAt});return{status:'failed',queueId:row.queueId,recovered:false,item:updated,oldFailure:originalFailure};}

export async function repairInstagramReviewQueue({limit=MAX_REPAIRS,now=Date.now(),queueId=null}={}){if(queueId)return repairInstagramReviewItem(queueId,{now});const all=await listJson(INDEX);const ambiguous=all.filter(row=>['PUBLISHING','PUBLISH_AMBIGUOUS'].includes(String(row?.status||'').toUpperCase())&&!row?.postedAt).slice(0,MAX_REPAIRS);const reconciled=[];for(const row of ambiguous){try{const result=await reconcilePublishedRow(row,{now});if(result)reconciled.push(result);}catch(error){console.error('[instagram-review-repair] post-publish reconciliation failed',row?.queueId,error);}}const rows=all.filter(row=>repairable(row,now)).slice(0,Math.max(1,Math.min(MAX_REPAIRS,Number(limit)||MAX_REPAIRS)));const diagnostics={examined:rows.length,attempted:0,recovered:0,stillInvalid:0,articleNotFound:0,canonical200:0,canonicalTimeout:0,canonicalOther:0,canonicalMs:0,imageFallback:0,cardFailed:0,captionFailed:0,storageFailed:0,skippedBackoff:0,postPublishReconciled:reconciled.length,reconciledItems:reconciled};const repaired=[];for(const row of rows){diagnostics.attempted++;try{const result=await repairInstagramReviewItem(row.queueId,{now});if(result.status==='skipped'){diagnostics.skippedBackoff++;continue;}if(result.recovered)diagnostics.recovered++;else{diagnostics.stillInvalid++;const code=String(result.item?.failureCode||'');if(code==='ARTICLE_NOT_FOUND')diagnostics.articleNotFound++;if(code.startsWith('CARD_'))diagnostics.cardFailed++;if(code==='CAPTION_FAILED')diagnostics.captionFailed++;}if(result.item)repaired.push(result.item);}catch(error){diagnostics.stillInvalid++;diagnostics.storageFailed++;console.error('[instagram-review-repair] item failed',row.queueId,error);}}const summary={...diagnostics,repairedAt:new Date().toISOString(),items:repaired.map(row=>({queueId:row.queueId,status:row.status,failureCode:row.failureCode||null,repairResult:row.repairResult||null,canonicalUrl:row.canonicalUrl||null,failedSlide:row.failedSlide||null,nextRetryAt:row.nextRetryAt||null}))};try{await setJson('ba:social:instagram:review:repair:last-run',summary);}catch{}return summary;}
