const FOREIGN_PUBLISHERS=new Set(['Deutsche Welle','NASA','NASA JPL','NOAA NHC','U.S. National Science Foundation','U.S. Geological Survey','TechCrunch','The Verge']);
const ID_MARKERS=['yang','dan','untuk','dengan','pada','dalam','ini','itu','adalah','karena','dapat'];
const EN_TITLE_MARKERS=['the','and','for','with','from','this','that','were','have','has','into','after','before','about','over','under','during','while','where','which','will','their','there','they'];
function countWords(text,words){return words.reduce((n,word)=>n+(new RegExp(`\\b${word}\\b`,'g').test(text)?1:0),0)}
function hasIndonesianSurface(article={}){const head=String(`${article.title||''} ${article.excerpt||''}`).toLowerCase();const body=String(article.content||'').slice(0,600).toLowerCase();return countWords(head,ID_MARKERS)>=1&&countWords(body,ID_MARKERS)>=1&&countWords(String(article.title||'').toLowerCase(),EN_TITLE_MARKERS)<2}
export function isIndexableArticle(article={}){const rawStatus=String(article.translationStatus||'').toLowerCase(),language=String(article.language||'id').toLowerCase();if(rawStatus==='pending'||rawStatus==='failed'||rawStatus==='fallback_original'||language!=='id')return false;if(article.contentType==='article')return article.status==='PUBLISHED'&&article.indexable!==false&&!/^noindex/i.test(String(article.robots||''));if(rawStatus==='translated')return article.indexable!==false&&!/^noindex/i.test(String(article.robots||''))&&hasIndonesianSurface(article);if(article.indexable===false||/^noindex/i.test(String(article.robots||'')))return false;return hasIndonesianSurface(article)}
export function buildArticleSEO(article={}){const slug=String(article.slug||'artikel'),canonical=`https://berita-auto.vercel.app/artikel/${encodeURIComponent(slug)}`;return{metaTitle:String(article.metaTitle||article.title||'').slice(0,60),metaDescription:String(article.metaDescription||article.excerpt||'').slice(0,160),canonical,robots:article.status==='PUBLISHED'?'index, follow':'noindex, nofollow',schemaType:article.articleType==='HOW_TO'&&Array.isArray(article.steps)&&article.steps.length?'HowTo':'Article',breadcrumb:['Beranda','Artikel',article.title||'Artikel']}}
export {FOREIGN_PUBLISHERS,hasIndonesianSurface};
