import {categories} from '../lib/categories.js';
export default function Header({latestTitle=''}){return <><header className="site-header"><div className="header-inner"><a className="brand" href="/"><span className="brand-mark">BA</span><span>Berita <b>Auto</b></span></a><div className="live-badge"><i/> LIVE · UPDATE OTOMATIS</div><nav className="desktop-nav" aria-label="Navigasi utama">{categories.slice(0,9).map(c=><a key={c} href={`/kategori/${c.toLowerCase()}`}>{c}</a>)}</nav><form className="search-form" action="/cari"><label className="sr-only" htmlFor="site-search">Cari berita</label><input id="site-search" name="q" placeholder="Cari berita…"/><button type="submit" aria-label="Cari">⌕</button></form><details className="mobile-menu"><summary aria-label="Buka menu">☰</summary><div className="mobile-menu-panel"><form className="mobile-search" action="/cari"><label className="sr-only" htmlFor="mobile-site-search">Cari berita</label><input id="mobile-site-search" name="q" placeholder="Cari berita…"/><button type="submit">Cari</button></form>{categories.map(c=><a key={c} href={`/kategori/${c.toLowerCase()}`}>{c}</a>)}</div></details></div></header>{latestTitle&&<div className="latest-bar"><div className="container latest-inner"><strong>BERITA TERKINI</strong><a href="/">{latestTitle}</a></div></div>}</>}
