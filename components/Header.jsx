import {categories} from '../lib/categories.js';
export default function Header({latestTitle=''}){return <><header className="site-header"><div className="header-inner"><a className="brand" href="/">Berita <span>Auto</span></a><div className="live-badge"><i/> LIVE · UPDATE OTOMATIS</div><nav className="desktop-nav" aria-label="Kategori utama">{categories.slice(0,6).map(c=><a key={c} href={'/kategori/'+c.toLowerCase()}>{c}</a>)}</nav><form className="search-form" action="/cari"><label className="sr-only" htmlFor="site-search">Cari berita</label><input id="site-search" name="q" placeholder="Cari berita…"/><button aria-label="Cari">⌕</button></form><button className="menu-button" type="button" aria-label="Buka menu">☰</button></div></header>{latestTitle&&<div className="latest-bar"><div className="container latest-inner"><strong>TERBARU</strong><a href="/">{latestTitle}</a></div></div>}</>}
