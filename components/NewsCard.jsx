import {articlePath} from '../lib/article-url.js';
import {cleanExcerpt} from '../lib/text.js';
function formatDate(value){if(!value)return '';const d=new Date(value);if(Number.isNaN(d.getTime()))return '';return new Intl.DateTimeFormat('id-ID',{day:'numeric',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit',timeZone:'Asia/Jakarta'}).format(d)+' WIB';}
export default function NewsCard({article,featured=false}){const excerpt=cleanExcerpt(article.excerpt||article.summary||'');const published=article.sitePublishedAt||article.publishedAt||article.createdAt;return <article className={`news-card${featured?' featured':''}`}><div className="news-card-image-wrap">{article.imageUrl?<img className="news-card-image" src={article.imageUrl} alt={article.title} width={1200} height={800} loading={featured?'eager':'lazy'}/>:<div className="image-fallback" aria-hidden="true"><span>BA</span></div>}</div><div className="news-card-body"><div className="news-card-meta"><a className="category-chip" href={`/kategori/${(article.category||'Nasional').toLowerCase()}`}>{article.category||'Nasional'}</a><span className="news-card-time"><small>Terbit di Berita Auto</small><time dateTime={published}>{formatDate(published)}</time></span></div><h3 className="news-card-title"><a href={articlePath(article)}>{article.title}</a></h3>{!featured&&<p className="news-card-excerpt">{excerpt}</p>}{article.sourceName&&<small className="news-card-source">{article.sourceName}</small>}</div></article>}
