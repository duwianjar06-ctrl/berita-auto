import {articlePath} from '../lib/article-url.js';
import {cleanExcerpt} from '../lib/text.js';
function formatDate(value){if(!value)return '';const d=new Date(value);if(Number.isNaN(d.getTime()))return '';return new Intl.DateTimeFormat('id-ID',{day:'numeric',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit',timeZone:'Asia/Jakarta'}).format(d)+' WIB';}
export default function NewsCard({article,featured=false}){const excerpt=cleanExcerpt(article.excerpt||article.summary||'');return <article className={`news-card${featured?' featured':''}`}><div className="news-card-image-wrap">{article.imageUrl?<img className="news-card-image" src={article.imageUrl} alt={article.title} width={1200} height={800} loading={featured?'eager':'lazy'}/>:<div className="image-fallback" aria-hidden="true"><span>BA</span></div>}</div><div className="news-card-body"><div className="news-card-meta"><a className="category-chip" href={`/kategori/${(article.category||'Nasional').toLowerCase()}`}>{article.category||'Nasional'}</a><span>{formatDate(article.publishedAt||article.createdAt)}</span></div><h3 className="news-card-title"><a href={articlePath(article)}>{article.title}</a></h3>{!featured&&<p className="news-card-excerpt">{excerpt}</p>}{article.sourceName&&<small className="news-card-source">{article.sourceName}</small>}</div></article>}
