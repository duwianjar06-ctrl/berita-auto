import {articlePath} from '../lib/article-url.js';
function formatDate(value){if(!value)return '';const d=new Date(value);if(Number.isNaN(d.getTime()))return '';return new Intl.DateTimeFormat('id-ID',{day:'numeric',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit',timeZone:'Asia/Jakarta'}).format(d)+' WIB';}
export default function NewsCard({article,featured=false}){return <article className={featured?'news-card featured':'news-card'}>{article.imageUrl?<img src={article.imageUrl} alt={article.title} width={1200} height={800} loading={featured?'eager':'lazy'}/>:<div className="image-fallback" aria-hidden="true"><span>BA</span></div>}<div className="news-card-body"><a className="category-label" href={'/kategori/'+(article.category||'Nasional').toLowerCase()}>{article.category||'Nasional'}</a><h3><a href={articlePath(article)}>{article.title}</a></h3><p>{article.excerpt||article.summary||''}</p><small>{formatDate(article.publishedAt||article.generatedAt)}{article.sourceName?' · '+article.sourceName:''}</small></div></article>}
