import assert from 'node:assert/strict';
import sharp from 'sharp';
import {randomBytes} from 'node:crypto';
import {validateArticleForInstagram,validateSourceImage,prepareInstagramCandidate,finalRevalidatePreparedCandidate} from '../lib/social-preparation.js';
import {validateSocialCardBuffer} from '../lib/social-card-validation.js';
import {readFileSync} from 'node:fs';
const socialRun=readFileSync(new URL('./social-run.js',import.meta.url),'utf8');
const review=readFileSync(new URL('../lib/instagram-review.js',import.meta.url),'utf8');
const repair=readFileSync(new URL('../lib/instagram-review-repair.js',import.meta.url),'utf8');
const admin=readFileSync(new URL('../lib/admin-instagram.js',import.meta.url),'utf8');
const dashboard=readFileSync(new URL('../app/admin-instagram/InstagramDashboard.jsx',import.meta.url),'utf8');
const cardValidation=readFileSync(new URL('../lib/social-card-validation.js',import.meta.url),'utf8');
const jpeg=await sharp(randomBytes(1080*1350*3),{raw:{width:1080,height:1350,channels:3}}).jpeg({quality:86}).toBuffer();
function response({status=200,type='image/jpeg',body=jpeg}={}){return new Response(body,{status,headers:{'content-type':type,'x-social-card-render':'valid','x-social-card-text-length':'30'}});}
const article={id:'a10000000000000000000001',stableId:'a10000000000000000000001',slug:'berita-test',title:'Judul berita yang valid',category:'Teknologi',publisher:'ANTARA',sitePublishedAt:'2026-08-16T12:00:00Z',sourcePublishedAt:'2026-08-16T11:50:00Z',sourceUrl:'https://example.com/berita-test',canonicalUrl:'https://berita-auto.vercel.app/berita/berita-test',imageUrl:'https://example.com/image.jpg',imageProvenance:'article_metadata',excerpt:'Ringkasan berita yang cukup panjang untuk social card.'};
const validFetch=async(url)=>{if(String(url).includes('/image.jpg'))return response();if(String(url).includes('/berita/'))return new Response('<html>ok</html>',{status:200,headers:{'content-type':'text/html'}});return response();};
const articleCheck=await validateArticleForInstagram(article,{fetchImpl:validFetch});assert.equal(articleCheck.valid,true);assert.equal(articleCheck.publicStatus,'PASS');assert.equal(articleCheck.sourceUrl,article.sourceUrl);assert.ok(articleCheck.canonicalAttempts>=1);
const imageCheck=await validateSourceImage(article,{fetchImpl:validFetch});assert.equal(imageCheck.status,'MATCHED_ARTICLE_METADATA');assert.equal(imageCheck.width,1080);assert.equal(imageCheck.height,1350);
const htmlFetch=async()=>new Response('<html>not an image</html>',{status:200,headers:{'content-type':'text/html'}});const fallback=await validateSourceImage(article,{fetchImpl:htmlFetch});assert.equal(fallback.status,'FALLBACK_USED');assert.match(fallback.reason,/image_content_type/);
const timeoutFetch=async(_url,{signal})=>await new Promise((resolve,reject)=>{signal.addEventListener('abort',()=>reject(Object.assign(new Error('aborted'),{name:'AbortError'})));});const timed=await validateSourceImage(article,{fetchImpl:timeoutFetch,timeoutMs:1});assert.equal(timed.status,'FALLBACK_USED');assert.equal(timed.reason,'image_timeout');
let canonicalCalls=0;const canonicalTimeoutThenPass=async(url,options)=>{if(String(url).includes('/image.jpg'))return response();if(String(url).includes('/berita/')){canonicalCalls++;if(canonicalCalls===1){return await new Promise((resolve,reject)=>{options.signal.addEventListener('abort',()=>reject(Object.assign(new Error('aborted'),{name:'AbortError'})));});}return new Response('<html>ok</html>',{status:200,headers:{'content-type':'text/html'}});}return response();};
const repairedCanonical=await validateArticleForInstagram(article,{fetchImpl:canonicalTimeoutThenPass,timeoutMs:1});assert.equal(repairedCanonical.valid,true);assert.equal(repairedCanonical.failureCode,null);assert.equal(repairedCanonical.canonicalAttempts,2);
const cardUrl='https://blob.vercel-storage.com/social/test.jpg';
let cardCalls=0;const cardTimeoutThenPass=async(url,options)=>{if(String(url).includes('/berita/'))return new Response('<html>ok</html>',{status:200,headers:{'content-type':'text/html'}});if(String(url).includes('social/test.jpg')){cardCalls++;if(cardCalls===1){return await new Promise((resolve,reject)=>{options.signal.addEventListener('abort',()=>reject(Object.assign(new Error('aborted'),{name:'AbortError'})));});}return response();}if(String(url).includes('/image.jpg'))return response();return response();};
const prepared=await prepareInstagramCandidate({...article,cardUrls:[cardUrl],previewUrl:cardUrl,render:{status:'RENDER_SUCCESS',width:1080,height:1350,format:'jpeg',bytes:jpeg.length}},{siteUrl:'https://berita-auto.vercel.app',full:false,fetchImpl:cardTimeoutThenPass});assert.equal(prepared.preparationState,'READY');assert.equal(prepared.cardReady,true);assert.equal(prepared.captionStatus,'READY');assert.equal(prepared.dedupeStatus,'NOT_POSTED');assert.equal(prepared.imageValidationStatus,'MATCHED_ARTICLE_METADATA');
const final=await finalRevalidatePreparedCandidate({article,cardUrls:[cardUrl],state:'queued',preparationState:'READY'},{siteUrl:'https://berita-auto.vercel.app',fetchImpl:validFetch});assert.equal(final.valid,true);
const visualSvg=`<svg xmlns="http://www.w3.org/2000/svg" width="1080" height="1350"><rect width="1080" height="1350" fill="#07111f"/><text x="72" y="300" fill="#fff" font-family="sans-serif" font-size="70">VALID CARD</text></svg>`;const visualJpeg=await sharp(Buffer.from(visualSvg)).jpeg().toBuffer();const visualCheck=await validateSocialCardBuffer(visualJpeg,{expectedTextLength:30});assert.equal(visualCheck.format,'jpeg');assert.equal(visualCheck.width,1080);assert.equal(visualCheck.height,1350);assert.ok(visualCheck.ranges.some(value=>value>2));
const blankDark=await sharp({create:{width:1080,height:1350,channels:3,background:{r:1,g:1,b:1}}}).jpeg().toBuffer();await assert.rejects(()=>validateSocialCardBuffer(blankDark,{expectedTextLength:30}),/social_card_blank_dark/);
assert.match(cardValidation,/PUBLIC_VERIFY_WARMUP_MS=350/);assert.match(cardValidation,/social_card_invalid_size/);assert.match(cardValidation,/social_card_invalid_dimensions/);
assert.match(review,/PREPARATION_ATTEMPT_BUDGET/);assert.match(review,/PREPARATION_READY_TARGET/);assert.match(review,/candidateAttempted/);assert.match(review,/skippedBackoff/);assert.match(review,/nextRetryAt/);assert.match(review,/continue/);assert.match(review,/CARD_PUBLIC_TIMEOUT/);assert.match(review,/CANONICAL_URL_TIMEOUT/);
assert.match(repair,/RETRY_DELAYS_MINUTES/);assert.match(repair,/nextRetryAt/);assert.match(repair,/filter\(row=>repairable/);assert.match(repair,/CARD_PUBLIC_TIMEOUT/);
assert.match(socialRun,/prepareDuringCooldown/);assert.match(socialRun,/prepareInstagramCandidates/);assert.match(socialRun,/getPublishingUsage/);assert.ok(socialRun.indexOf('async function prepareDuringCooldown')<socialRun.indexOf('const metaStarted'),'cooldown preparation must precede Meta usage');assert.match(socialRun,/primaryCandidate/);assert.match(socialRun,/standbyCandidates/);assert.match(socialRun,/finalRevalidatePreparedCandidate/);assert.match(socialRun,/reason:'already_published'/);assert.match(admin,/preparationState/);assert.match(admin,/imageValidationStatus/);assert.match(admin,/imageRelationship/);assert.match(dashboard,/Social Card/);assert.match(dashboard,/READY/);
console.log('Instagram preparation regression: PASS bounded canonical retry, warmed card public verification, robust card validation, failure isolation/backoff, primary/standby preparation, final revalidation, no Meta during cooldown');
