import {fetchNews} from '../lib/rss.js';import {generateArticle} from '../lib/ai.js';import {readArticles,writeArticles} from '../lib/storage.js';import {articleFingerprint} from './normalize.js';import {selectCandidates} from './strategy.js';
const old=await readArticles(),seen=new Set(old.map(a=>a.fingerprint));console.log('[worker] scheduled run');const fetched=await fetchNews();const items=fetched.map(i=>({...i,fingerprint:articleFingerprint(i)}));const candidates=selectCandidates(items,seen,3);console.log(`[candidates] new: ${candidates.length}`);const fresh=[];for(const item of candidates){try{console.log(`[publish] title: ${item.title}`);console.log(`[publish] source: ${item.sourceName||item.sourceUrl}`);const ai=await generateArticle(item);fresh.push({id:item.fingerprint,...item,...ai,createdAt:new Date().toISOString()});}catch(error){console.error(`[publish] failed: ${item.title}: ${error.message}`)}}await writeArticles([...fresh,...old].slice(0,200));console.log(`[publish] selected: ${fresh.length}`);console.log('[worker] complete');
