import {readArticles} from '../lib/storage.js';
import {persistenceConfigured,acquireLock,releaseLock,getJson,setJson} from '../lib/persistence.js';
import {instagramConfigured,instagramConfig,createMediaContainer,createCarouselContainer,pollContainerReady,publishMediaContainer,getPublishingUsage,classifyInstagramError} from '../lib/instagram.js';
import {SOCIAL_LOCK_KEY,SOCIAL_LOCK_TTL,socialConfig,shouldSkipCooldown,shouldSkipDailyLimit,shouldSkipMetaBuffer,queueSocialArticle,readSocialQueue,readRecentPublished,selectBestSocialArticle,deterministicCaption,getDailyPublishedCount,getLastPublishedAt,markSocialProcessing,markSocialFailure,markSocialPublished,incrementDailyPublishedCount} from '../lib/social.js';
import {buildSocialSlides} from '../lib/social-visual.js';
import {buildCarouselImageUrls} from '../lib/social-carousel.js';
async function validatePublicImage(url){const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),10000);try{const response=await fetch(url,{method:'GET',headers:{Range:'bytes=0-64'},signal:controller.signal,cache:'no-store',redirect:'follow'});const type=String(response.headers.get('content-type')||'').toLowerCase();if(!response.ok||!type.startsWith('image/'))throw new Error(`social_card_invalid_response_${response.status}_${type||'missing_content_type'}`);return true;}catch(error){throw Object.assign(new Error(String(error?.message||error).slice(0,200)),{kind:'permanent'});}finally{clearTimeout(timer);}}
function retryDelay(attempts){return Math.min(30*60*1000,Math.max(60*1000,2**Math.min(attempts,5)*60*1000));}
async function createAndPublishMedia(article,caption,siteUrl){const slides=buildSocialSlides(article);const urls=buildCarouselImageUrls(siteUrl,article.id,slides.length);for(const url of urls)await validatePublicImage(url);if(slides.length===1){const containerId=await createMediaContainer({imageUrl:urls[0],caption});const ready=await pollContainerReady(containerId);if(!ready.ready)return{ready,containerId};return{ready,containerId,mediaId:await publishMediaContainer(containerId),carousel:false};}const childIds=[];for(const url of urls){const child=await createMediaContainer({imageUrl:url,isCarouselItem:true});childIds.push(child);}for(const childId of childIds){const ready=await pollContainerReady(childId);if(!ready.ready)return{ready,childIds,carousel:true};}const containerId=await createCarouselContainer({children:childIds,caption});const ready=await pollContainerReady(containerId);if(!ready.ready)return{ready,containerId,childIds,carousel:true};return{ready,containerId,childIds,mediaId:await publishMediaContainer(containerId),carousel:true};}
export async function runSocialCycle({trigger='manual',now=Date.now()}={}){if(!instagramConfig().enabled)return{status:'skipped',reason:'disabled'};if(!instagramConfigured())return{status:'skipped',reason:'missing_configuration'};if(!persistenceConfigured())return{status:'skipped',reason:'persistence_not_configured'};const lockToken=`${trigger}:${process.pid}:${now}`;if(!(await acquireLock(SOCIAL_LOCK_KEY,lockToken,SOCIAL_LOCK_TTL))){console.log('[social] skipped reason=lock_busy');return{status:'skipped',reason:'lock_busy'}}try{const cfg=socialConfig();const articles=await readArticles();for(const article of articles.slice(0,100)){if(article?.id&&article?.sitePublishedAt)await queueSocialArticle(article).catch(error=>console.warn(`[social] queue_reconcile_failed articleId=${article.id} reason=${String(error?.message||error).slice(0,120)}`));}const queue=await readSocialQueue(100);const eligible=queue.filter(item=>{const article=articles.find(a=>a.id===item.articleId);return article&&article.sitePublishedAt&&item.state!=='published';}).map(item=>({...item,article:articles.find(a=>a.id===item.articleId)}));if(!eligible.length){console.log('[social] skipped reason=queue_empty');return{status:'skipped',reason:'queue_empty'}}const recent=await readRecentPublished(20);const last=await getLastPublishedAt();if(shouldSkipCooldown(last,now,cfg.minIntervalMinutes)){console.log('[social] skipped reason=cooldown');return{status:'skipped',reason:'cooldown'}}const daily=await getDailyPublishedCount(now);if(shouldSkipDailyLimit(daily,cfg.maxPostsPerDay)){console.log('[social] skipped reason=daily_limit');return{status:'skipped',reason:'daily_limit',publishedToday:daily}}const usage=await getPublishingUsage();if(shouldSkipMetaBuffer(usage,cfg.limitBuffer)){console.log('[social] skipped reason=meta_limit_buffer');return{status:'skipped',reason:'meta_limit_buffer',remaining:usage.remaining}}const selected=selectBestSocialArticle(eligible,{now,recentPublished:recent});if(!selected){console.log('[social] skipped reason=no_worthy_content');return{status:'skipped',reason:'no_worthy_content'}}const publishedState=await getJson(`ba:social:instagram:published:${selected.articleId}`);if(publishedState){console.log(`[social] skipped reason=already_published articleId=${selected.articleId}`);return{status:'skipped',reason:'already_published'}}const processing=await markSocialProcessing(selected);const article=selected.article;console.log(`[social] selected articleId=${article.id} score=${selected.selectionScore}`);try{const caption=deterministicCaption(article,instagramConfig().siteUrl);const result=await createAndPublishMedia(article,caption,instagramConfig().siteUrl);processing.containerId=result.containerId;processing.carousel=Boolean(result.carousel);processing.childContainerIds=result.childIds||[];await setJson(`ba:social:instagram:item:${processing.articleId}`,{...processing,updatedAt:new Date().toISOString()});if(!result.ready.ready){const err=result.ready.permanent?new Error('instagram_media_processing_failed'):new Error('instagram_media_processing_pending');if(result.ready.permanent){await markSocialFailure(processing,err,{permanent:true});console.warn(`[instagram] failed articleId=${article.id} reason=media_processing_failed`);return{status:'skipped',reason:'media_processing_failed'}}await markSocialFailure(processing,err,{retryAfterMs:retryDelay(processing.attempts)});console.log(`[instagram] failed articleId=${article.id} reason=media_processing_pending`);return{status:'retry',reason:'media_processing_pending'}}const mediaId=result.mediaId;const publishedAt=new Date().toISOString();let record=null;let persistenceError=null;for(let attempt=1;attempt<=3;attempt++){try{record=await markSocialPublished(processing,{mediaId,publishedAt});break;}catch(error){persistenceError=error;if(attempt<3)await new Promise(resolve=>setTimeout(resolve,500*attempt));}}if(!record){console.error(`[social] published_state_persist_failed articleId=${article.id} reason=${String(persistenceError?.message||persistenceError).slice(0,120)}`);return{status:'persist_error',articleId:article.id,mediaId}}let publishedToday=daily+1;try{publishedToday=await incrementDailyPublishedCount(now)}catch(error){console.warn(`[social] daily_counter_update_failed articleId=${article.id} reason=${String(error?.message||error).slice(0,120)}`)}console.log(`[instagram] published articleId=${article.id} mediaId=${mediaId}${result.carousel?' type=carousel':''}`);return{status:'published',articleId:article.id,slug:article.slug,mediaId,publishedAt,carousel:Boolean(result.carousel),slideCount:buildSocialSlides(article).length,queueRemaining:Math.max(0,eligible.length-1),publishedToday,record}}catch(error){const classification=classifyInstagramError(error);const permanent=classification.kind==='permanent';await markSocialFailure(processing,error,{permanent,retryAfterMs:retryDelay(processing.attempts)});if(classification.reason==='auth_token_invalid')console.warn('[instagram] failed reason=instagram_auth_failed');else console.warn(`[instagram] failed articleId=${article.id} reason=${classification.reason}`);return{status:permanent?'failed':'retry',articleId:article.id,reason:classification.reason}}}finally{await releaseLock(SOCIAL_LOCK_KEY,lockToken).catch(()=>{})}}
if(process.argv[1]?.endsWith('/worker/social-run.js'))runSocialCycle({trigger:'cli'}).then(result=>console.log(JSON.stringify(result))).catch(error=>{console.error(`[social] fatal ${String(error?.message||error).slice(0,240)}`);process.exitCode=1});
