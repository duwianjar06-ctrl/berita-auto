import assert from 'node:assert/strict';
import fs from 'node:fs';
import {fileURLToPath} from 'node:url';
import {dirname,join} from 'node:path';
import {extractArticleSubject,isMalformedArticleQuery,sanitizeArticleQuery} from '../lib/article-ideas.js';
const root=dirname(dirname(fileURLToPath(import.meta.url)));
const read=path=>fs.readFileSync(join(root,path),'utf8');
const category=read('components/CategoryNavGrouped.jsx');
assert.match(category,/const isArticlePath=pathname=>/);
assert.match(category,/const pathname=usePathname\(\);/);
assert.doesNotMatch(category,/const articleActive\s*=\s*pathname/);
assert.match(read('components/homepage-carousel-activity.js'),/typeof IntersectionObserver==='undefined'/);
const admin=read('app/admin-berita/AdminWorkspace.jsx');
assert.match(admin,/import \{articlePath\} from '..\/\.\/.\/lib\/article-url\.js';/);
assert.match(admin,/Buka Berita/);
assert.match(admin,/Cek Index/);
assert.match(admin,/Detail Admin/);
assert.match(admin,/canonicalUrl/);
const subject=extractArticleSubject('Penyebab Speedometer Motor Mati dan Cara Mengatasinya');
assert.equal(subject,'speedometer motor');
assert.equal(sanitizeArticleQuery('tanda tanda tanda tanda').valid,false);
assert.equal(isMalformedArticleQuery('cara cara menghemat listrik'),true);
const ideas=read('lib/article-ideas.js');
assert.match(ideas,/function deterministicGapExpansion/);
assert.match(ideas,/extractArticleSubject\(x\.topicCluster\|\|x\.title\)/);
assert.match(ideas,/repeatedMeaningfulToken/);
const pipeline=read('lib/article-pipeline.js');
assert.doesNotMatch(pipeline,/candidatePool\.slice\(0,5\)/);
assert.match(pipeline,/candidateDiagnostics/);
assert.match(pipeline,/for\(let i=0;i<candidatePool\.length;i\+\+\)/);
const image=read('lib/article-image.js');
assert.match(image,/checkArticleImageStorageHealth/);
assert.match(image,/generationStatus/);
assert.match(image,/persistenceStatus/);
assert.match(image,/STORE_SUSPENDED/);
const bulk=read('app/api/admin/instagram/review/route.js');
assert.match(bulk,/bulk_delete_state_not_allowed/);
assert.match(bulk,/requested,deleted,skipped,failed,failures/);
assert.match(bulk,/removeInstagramPublishQueueItem/);
assert.match(bulk,/status==='POSTED'/);
const social=read('app/api/cron/social-publish/route.js');
assert.match(social,/metaErrorCode/);
assert.match(social,/metaErrorSubcode/);
const telemetry=read('lib/article-automation-telemetry.js');
assert.match(telemetry,/candidateDiagnostics/);
console.log('emergency-runtime-automation-regression: PASS');
