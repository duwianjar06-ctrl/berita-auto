async function openGraphImage(url){try{const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),10000);const res=await fetch(url,{signal:controller.signal,headers:{'user-agent':'BeritaAuto/1.0'}});clearTimeout(timer);if(!res.ok)return null;const html=await res.text();for(const re of [/property=[\"']og:image[\"'][^>]*content=[\"']([^\"']+)/i,/name=[\"']twitter:image[\"'][^>]*content=[\"']([^\"']+)/i,/content=[\"']([^\"']+)[\"'][^>]*property=[\"']og:image[\"']/i]){const m=html.match(re);if(m){try{const u=new URL(m[1],url);if(/^https?:$/.test(u.protocol))return u.href;}catch{}}}}catch{}return null;}
export async function enrichArticle(item){if(item.imageUrl)return {imageUrl:item.imageUrl,source:'rss'};const image=await openGraphImage(item.url);return {imageUrl:image,source:image?'og':'none'};}
export async function backfillArticles(items,limit=20){let checked=0,updated=0;for(const article of items.slice(0,limit)){if(article.imageUrl||!article.url)continue;checked++;const image=await openGraphImage(article.url);if(image){article.imageUrl=image;updated++;}}return {checked,updated};}
